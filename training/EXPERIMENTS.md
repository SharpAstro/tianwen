# Experiments ledger

One line per run or measurement, oldest first: what was trained or measured, on which prepared
cache, how many seeds, the verdict. Negatives are included, and a result that needed a new
explanation after the fact is recorded as a finding rather than a conclusion. The full run records
(logs, checkpoints, figures, and each run script with its pre-registered predictions) stay under
`D:\Astro-Dataset\n2n-smoke\<gen>\`; the narrative is `docs/plans/osc-narrowband-denoiser.md`
sections 1a to 1o and `docs/plans/denoiser-training.md`. A new run appends one row here and names
the commit it ran from.

| gen | date | cache | what | seeds | verdict |
|---|---|---|---|---|---|
| l1, l2, a_up, a_avg, v2 to v8 | 2026-08-12 | n2n (calgated; 8 train + 2 val sessions, 960 cells) | smoke ablations: L1 vs L2, transposed conv vs upsample, 4v4 averaging, DoG band loss at three weights, noise conditioning with mixed levels | 1, unseeded | conditioning is what keeps faint stars (amplitude kept 0.75 / 0.62 for v4 / v8 against 0.10 / 0.07 for plain L1 / L2); fabricated point sources per tile 26 to 209 against the raw sub's own 22 |
| v9 | 2026-08-13 | n2n-ds | half-master pairs against a matched control on one shared cache | 1, unseeded | RETRACTED: torch was never seeded, so the positive was a draw |
| v10 to v13 | 2026-08-13 to 14 | n2n-ds | the seeded verdict on half-master pairs, and the gate (fabrication bar, noise, faint amplitude) with its stopping rule | 3 per arm, 15 runs | half pairs null across seeds; the gate selects mid-schedule checkpoints; every run bit-identical on re-run |
| v14 | 2026-08-14 | n2n-ds | per-band noise conditioning (`--cond-bands`), and half pairs underneath it | 3 x 2 arms | bands no better than the scalar plane; half pairs null again, two of three never passed the gate |
| v15 | 2026-08-14 | n2n-ds | `--gate-observe`: the val session the gate does not select on, probed on the same schedule | 3 | selections reproduced v13 exactly; the probed session is the stricter of the two by luck of ordering |
| v16 | 2026-08-14 | n2n-ds | genuinely short schedules (1,600 and 2,400 steps) against the truncated long one | 3 x 2 | NEGATIVE: the mid-schedule pick is a hot-learning-rate artefact of cosine annealing, not a budget finding |
| v17, v17b, v17c | 2026-08-15 | n2n-big (60 sessions) | the first run sized to produce a model: 60 sessions, base 32 and 48; the two controls; the fabrication question closed | 1 + 3 + 3 | the budget was wrong and v16 said why; v17c (60 sessions, base 32) becomes the large-pool control |
| v18 | 2026-08-16 | n2n-a52, n2n-b52 | drop the 8 ASI585 sessions against dropping 8 random ones | 3 x 2 | the ASI585 sessions are innocent; dropping 8 of anything changes nothing |
| v19 | 2026-08-16 | n2n-c21, n2n-d8 | a 2x2 on session count against cells per session, nested in v17's 60 | 3 x 2 | read as "session COUNT is the lever"; v19d seed 2 (8 sessions x 45 cells) is the checkpoint that ships |
| v20 | 2026-08-16 | n2n-eval4 | observer rotation over every unseen session; no training | none | the few-session arms win on all four observers, including the rig they never saw |
| v21 | 2026-08-16 | n2n-big, n2n-d8 | band conditioning at 60 and at 8 sessions | 3 x 2 | no rescue of the 60-session arm, and it destabilises training |
| v22 | 2026-08-16 | n2n-big, n2n-eval4 | four measurements on existing checkpoints (paircorr, keptshared, structfrontier, structkeep); no training | none | the N2N premise of independent pair noise is broken in every session, not in a bad subset; the few-session operator property |
| v23 | 2026-08-16 | n2n-e8 | armE: eight different sessions to armD's recipe, matched on camera | 3 | PA, PB and PC all failed; "session count is the lever" does not survive |
| v24 | 2026-08-16 | n2n-f8 | armF: a third disjoint eight, screened for content | 3 | no recipe: the 8-session outcome is a high-variance draw (0.825 / 0.726 / 0.739 across the three draws) |
| ship | 2026-08-17 | n2n-d8 | export v19d seed 2 to ONNX (opset 17, sigma baked in), the dial study, the cross-language parity fixture | none | shipped opt-in; the blend is the dial and the conditioning dial is rejected; a per-channel level prior found and corrected by `RestoreLevel` |
| E0.5 | 2026-09-02 | none (C#, the 163-sub Bubble master) | H0: the runner fed linear pixels to a net trained on stretched tiles | none | CONFIRMED and fixed; the verbatim path had cut every star peak by 30 percent at every SNR; `docs/plans/denoiser-training.md` section 9 |
| E0 | 2026-09-02 | n2n-d8 | this directory reproduces `n2n_v19d_s2_final.pt` from the cache (`denoise/repro-v19d.ps1`, 12.3 min on the 1070) | 1 (seed 2) | PASSED: both checkpoints (gate-selected step 1500, score 0.709; final step 4000) reproduce all 813,251 parameters bit for bit, and the log matches the 2026-08-16 run at every printed loss and gate figure. The script's first verdict read DIFFERENT because it hashed the FILES: `torch.save` names every archive member after the output stem and the trainer now records one more key (`pair_time`); re-saving the reference with that key under the repro's name gave the repro's hash exactly. Checkpoints are compared tensor for tensor now (`n2n_ckpt_equal.py`), never by hash |
| E1 | 2026-09-03 | none yet (the exporter is C#; `deg-*` caches are per arm) | the shared degradation exporter (`tianwen dataset degrade`) and the measurement that makes H2 testable: band1/band0 of injected draws against the bake's own real pairs, one implementation for both | none | SHIPPED, and the injector needed calibrating rather than merely building. Real sub pairs read 0.786 and real half-master pairs 0.463 on `2025-2026-organized` (64 pairs each); white injection reads 0.216 and a bilinear-warped one only 0.328, so NEITHER arm as first built reached the deployed shape. `--warp-sigma`, standing in for the whole correlating chain (the demosaic, not just the registrar's warp), lands on 0.460 at 0.5 px and on a sub's 0.786 at 0.8 px. Run E2's arms as S-white against S-warped AT 0.5, and re-measure the sigma per bake. Clean-tile parity against the bake's own P0 tiles: 0.0 on every session |
| E0 re-check | 2026-09-03 | n2n-d8 | the E0 acceptance re-run after the trainer gained `--synthetic` and `--prepare` gained the `injected` key | 1 (seed 2) | IDENTICAL: 30 tensors, all 813,251 parameters, on BOTH checkpoints. A flag that only branches when passed cannot move the rng stream, and this is the cheap way to know rather than to argue it |
| E2 (aborted, relaunched) | 2026-09-03 | n2n-e2-white (discarded) | first launch of the two injection arms | 1 of 6 started | ABORTED after one seed and re-exported: the injected depth range's fixed 0.1 floor sat ABOVE the master depth of 34 of the 51 organized sessions (1/sqrt(StackedFrames), median 0.089, min 0.062), so the level the model is deployed at was outside the range it trained across. The floor is derived per session now. Kept as a row because the discarded arm is the evidence for the fix, and because the same trap is waiting in the deconvolver's PSF range: a fixed range against a per-session quantity |
| E2 | 2026-09-03 | n2n-e2-white, n2n-e2-warped, n2n-e2-control, n2n-eval4 | supervised synthetic injection against N2N at deployment depth: arms S-white and S-warped (three seeds each, identical cells and drawn depths, only the injected noise SHAPE differing), a matched same-pool N2N control, and the v19d controls, all scored in one run on half-master inputs against the other half | 3 x 3 arms + 3 re-scored | **H1 KILLED, H2 KILLED, H3 NOT SUPPORTED, and the design was underpowered for all three.** At MATCHED noise removed (each output blended back toward its input, which is the shipped strength dial), the twelve checkpoints' trade curves sit within a seed's noise of each other: between-regime sd 0.20 / 0.28 / 0.61 against within-regime sd 0.58 / 0.82 / 1.40 at 4 / 6 / 10 percent removed, and the best three models at every point are one each from three different regimes. Three confounds were caught before they became verdicts: comparing exchange RATES across models denoising at different strengths (the rate improves with strength in both control families); a per-channel level-bias separation with no overlap across three seeds each that dissolved entirely when matched; and gate-SELECTED controls scored against final-weights arms (v19d's selected checkpoints span 8.4 to 14.7 at 10 percent where its finals converge to 13.7 to 14.2). H2's second clause was mis-specified: a model that recognises correlated noise REMOVES it, so a residual still shaped like the input measures how little was done. E2b is NOT run as designed, being another three-seed arm where about 31 seeds are needed to read a one-point effect |
| depth | 2026-09-05 | n2n-eval4, n2n-e2-eval4b (the Gaia split, no training) | `gaia_depth.py`: the split's unmatched "detail" re-matched at BP 16, 18 and 21, the reverse direction per BP bin, and every unmatched peak classified by shape against the session's own Gaia-confirmed stars | none | **The BP 16 cap was the POOL's depth, not the field's, and the detail column was mostly stars on four of seven fields.** Horsehead: 13 percent confirmed at 16, 63 at 21 (floor 1.5 percent; the reverse direction agrees at 62), and of the 38 percent left 82 percent is broader than any confirmed star, the noise check at chance: 50 points stars, 31 nebulosity. Statue of Liberty 36 to 99.7; HIP 85088 71 to 86; HIP 34710 84 to 94. The 135 mm fields are unreadable past 16 (Rim floor 123 percent at BP 21) and eta Car's remainder is 83 percent star-shaped (blends). `n2n_starsplit.py` now takes the deepest per-session cap whose floor stays under 5 percent and reports stars / compact unmatched / extended, and only the extended column supports a structure claim |
| E8 exporter | 2026-09-05 | n2n-pairs (a P0-shaped pair cache from `2026-09-full`; not yet prepared) | the cross-night pair exporter (`tianwen dataset pair`) and its pair statistics: seven pairs (HD 74167 x1, Rim Nebula x6), the per-pair transform, per-channel gain, FWHM before and after, and the residual correlation between the two sides against the same-session halves through `n2n_pairstats.py` | none | SHIPPED, no training. Three findings before any arm runs. (1) The PSF decision belongs on the MASTERS: the sub table called HD 74167's two nights matched within 1 percent and the masters differ by 9 (1.80 against 1.98 px), because one is drizzled and one staged; five of seven pairs needed a 0.5 to 0.8 px Gaussian, and after it the sides still differ 3 to 6 percent (the profiles are not Gaussian). (2) Both sides pay the midpoint resampling equally, 1.85 to 2.15 px, so the pairs are softer than a deployed master by 16 percent. (3) The residual correlation statistic is dominated by shared faint SIGNAL: same-session halves read 0.06 to 0.27, the cross-night pairs 0.06 to 0.34 on the same sky, and the halves' 1.41x noise dilutes their leakage, so the two are not like for like and no shared-noise component is measurable this way. A first version of the mask selected on the combined pixel and read -0.23 on synthetic INDEPENDENT noise (conditioning on the sum being low anticorrelates the sides); the smoothed-scene mask reads 0.00 there and 0.70 on a half-shared control against a 0.71 truth. Vela SNR and HD 71272 found no quad fit (partial overlap; a WCS-seeded registration is the fix). H8's verdict must come from the arm; the kill criterion stands |
| E8 arm X | 2026-09-05 | n2n-x (six Rim Nebula pairs, 60 cells each, HD 74167 pair as the gate), n2n-x-ctl (same four Rim nights from `2026-09-full`, 360 cells, real subs, val on the two HD 74167 nights), scored on n2n-e2-eval4b and n2n-eval4 Horsehead + Statue | cross-night N2N (`--half-only`, the direction swapped per sample) against a pool-matched same-session control (`--mix-avg`), the E2 recipe, pre-registered in `run-x.ps1`; the per-session-cap split with the new full-strength and `--per-session` columns and a per-field re-score on eta Car | 3 + 3 | **KILLED, and the kill's inference does not follow.** No X seed passed a probe: 0.88 to 0.92x at 4000 with faint amplitude falling one for one (an attenuation); xctl passed at 1100 to 2000 (0.81 to 0.82x) then climbed into fabrication like v19d. Pooled on eval4b X reaches 7 to 10 percent removed and its extended column is the lowest in the table at 4 percent, which is the prediction's headline and is false: per field, X denoises eta Car alone (+14 to +20), leaves HIP-34710 and V1045 Ori (0 to +8), makes HIP-85088 noisier (-8 to 0) and ADDS 7 to 25 percent of noise on Horsehead and the Statue while brightening extended peaks 15 to 39 percent. On eta Car at matched 8 and 12 percent X costs more stars than xctl (12.1 to 14.5 against 9.9 to 12.5), the same compact, extended seed-scattered (0.2 to 6.2 around 3.3 to 5.2). Cause: the conditioning plane; one sky's master-depth halves span p5 0.22 to 0.54 against deployed 0.09 (Horsehead) to 0.83 (eta Car), the level trap of E2's first launch in a second coordinate, and the E2 injected inputs share the 0.23 floor (warped_s2 removes 2.8 percent on Horsehead + Statue, 20 pooled). X never learned the task, so the shared-noise ceiling is neither confirmed nor refuted; H8 PARKED behind PSF equality on the exported tiles, WCS-seeded registration for more skies, a covering conditioning range, and the three-frame shared-noise measurement. Side finding: full-strength removal is a per-FIELD number for every model, so a pooled matched point is not matched per field. First launch trained X alone (cache path built as `n2n-xctl`); control re-run after the fix |
| cond plane | 2026-09-05 | none (a measurement over `2026-09-full`) | `condpool.py`: every colour session's master placed on the conditioning plane against what the arms trained on. Per training session the injected input's p5 sits at 0.94 to 1.09 of its own master, so the E2 range covers deployment BY CONSTRUCTION and the POOL is what does not: master planes run 0.07 / 0.25 / 0.30 / 0.42 / 2.53 and **15 of 76 sit under the 0.23 the eight bottom out at** | 0 | FINDING: the pool is the lever, not the depth fraction, which cannot inject less than nothing |
| E9 wide | 2026-09-06 | n2n-e2-wide (E2's eight sessions plus nine low-plane ones, 45 cells each; val and export seed unchanged) | H9: does covering the low end of the conditioning plane fix the per-field collapse? The E2 recipe byte for byte, `tianwen dataset degrade --session` added so nineteen sessions export in 54 minutes | 3 | **WON.** 13.7 / 16.2 / 17.0 percent removed at full strength on eval4's two low-plane fields against a NINE-seed warped range of 2.8 to 11.3, inside warped's spread on the faint-structure columns, HIP-85088's collapse (6.9 and 7.6) gone at 18.0 to 26.8. Caveat recorded: 2 of 3 seeds fail the gate's 0.82x noise criterion, calibrated on models trained off the low end |
| three-frame | 2026-09-05 | pairs-triple (three Rim nights on one grid) | H8 precondition 4: `cov(A-B, A-C)` is a night's OWN deviation, split by spatial scale (`n2n_threeframe.py`) | 0 | 5.5 to 18.6 percent of the variance is structured, a LOWER bound (a fixed pattern is pixel-scale, and anything the three nights share cancels). Under the third the pre-registration named, so arm X2's confidence was raised before it ran |
| E8 arm X2 | 2026-09-06 | n2n-x2 (seven cross-night pairs, 45 cells each, eight injected draws per cell; HD 74167 held out) | H8 on ONE axis: identical inputs, cells and draws, only the target moved -- night B (independent noise) against night A (the night the input was degraded from) | 3 + 3 | **H8 REFUTED.** Every control seed removes noise (0.94 / 0.92 / 0.88x on the gate, 5.3 / 6.8 / 9.5 percent on eval4b); every independent-target seed ADDS it (1.07 / 1.04 / 1.07x, -9.5 / -6.2 / -6.2 percent), so no matched point exists. It amplifies rather than merely underperforming, which is a systematic being learned: a night carries a smooth field with a non-zero conditional mean, the part N2N is NOT unbiased against. Caveats recorded: the cache's conditioning floor is 0.33 (a draw can only add noise), and the target night is 1.2 to 4.7x noisier than the input's, which arm X2R reverses |
| E8 arm X2R | 2026-09-06 | n2n-x2r (the same seven pairs with the nights SWAPPED, so the target night is 1.19x the input's noise instead of 2.25x) | the one confound X2 could not exclude: is the independent target losing because it is INDEPENDENT or because it is NOISIER? | 3 + 3 | **Depth excluded, H8 refuted.** With the asymmetry nearly gone the independent-target arm gets WORSE (1.30 / 1.25 / 1.25x against X2's 1.07 / 1.04 / 1.07) and its control better (0.92 / 0.85 / 0.86x). On eval4b it removes -20.9 / -21.7 / -25.7 percent with an EXTENDED column of -67.6 / -67.6 / -71.4: manufacturing structure at nebulosity scale, which is the smooth per-night field seen directly |
| P2 E0 | 2026-09-06 | `2026-09-full` and `2025-2026-organized` PSF stores (a measurement, no cache) | the deconvolver's calibration step: is the store current, and what is the per-(train, filter, channel) (FWHM, beta) draw distribution? `PsfStoreVsCurrentDetectorProbe` on both bakes, then a conditional fit of log beta on FWHM | 0 | **No re-measure needed, and two calibration facts changed.** `2026-09-full` is already current-detector (count ratio 1.000, 0.000 px on 8 masters) where `2025-2026-organized` reproduces its documented staleness (1.038, +0.033 px), the old bake being the control that makes the zero readable rather than a self-comparison. The fit then found the pool is thin (10 of 17 (train, filter) cells hold ONE session; two cells carry 43 of 79) and that "beta correlates with FWHM r 0.66, sample jointly" is an ALL-CHANNELS POOLED statistic: it reproduces at +0.654 on its own store and splits to +0.508 / -0.238 / -0.104 per channel, so the joint draw holds on blue and red and not on green. 11 of 163 fits rail at `PsfProfileFit`'s `beta <= 25.0` grid edge and in 9 the Gaussian fits better (all 9 ch0), so a railed beta means Gaussian and must be excluded rather than clipped |
| P2 E1 (H5) | 2026-09-06 | `2026-09-full` session-masters, all 79, no cache | the deconvolver's conditioning contract: run the DEPLOYED `HfdPsfEstimator` over every retained master and encode its radius under the shipped range and three candidates (`PsfEncodingSpreadProbe`) | 0 | **H5's diagnosis AND its fix are both wrong.** The shipped `[1, 8]` does not collapse the archive (spread 0.293, 6 of 79 clamped, against a predicted "under 0.1"): that claim was true of the 51-session bake, and the full one runs 0.88 to 3.03 px radius. Lowering the floor to `[0.5, 8]` makes the spread SMALLER, 0.247, because psf01 is a ratio of logs and widening the span divides every difference by `log2(max/min)`; measured spreads match that formula to three decimals, which is what says the probe measures the encoding rather than the archive. The CEILING is the lever, nothing being near 8 px. `[0.5, 4.0]` is the pick (0.330, nothing clamped at either end, ceiling above the widest degraded input the exporter can make); `[0.75, 3.0]` spreads 0.495 and is rejected for clamping a real master today. The honest win is 13 percent more spread, not a transformation |
| D5 ship | 2026-09-06 | n2n-e2-wide (`e2_wide_s2.pt`, gate-selected at step 3100) | not a run: the D5 swap and its verification. `e2_wide_s2` replaces v19d as `tianwen_denoise_osc_e2wide_s2.onnx`, parity fixture regenerated, seam probe re-run against the COMMITTED file with no model-directory override, `*.onnx` returned to Git LFS | 0 | SHIPPED. The committed bytes reproduce D5's table exactly (cast spread 0.0188, stars 0.690 / 0.786 / 0.864 / 0.883, MAD 89 / 85 / 77 percent) and the C# path reproduces torch to 3.87e-7. Two findings about the tooling rather than the model: **the parity plate conditions the graph at sigma EXACTLY zero** on both checkpoints (the median-filled 16 px border plus the replicate pad leave 61 percent of the tile at one value), so it pins the deployment path but cannot exercise the sigma path and its std ratio is not a denoising number; and **the level prior weakened rather than vanishing** with the wider pool (cast 0.044 to 0.019 on the master, the plate's per-channel median deltas 0.00151 to 0.00072 in spread), so `RestoreLevel` stays unconditional. Not measured and now stated in the plan: the shipped model's training plane tops out near 2.5, so a single SUB is out of range on the high side where v19d was out of range on the low |
