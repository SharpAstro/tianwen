# Plan Implementation Summary

Status of every `PLAN-*.md` in the repo root, cross-checked against the codebase on 2026-05-16.

| Plan | Status |
|------|--------|
| [explorer-thumbnails](explorer-thumbnails.md) | **P0-P7 DONE (2026-09-02); P8 (a signed MSIX install) OPEN, waits for the next dispatch's package.** Windows Explorer thumbnails for `.fits .fit .fts .fz .ser`, for the Store viewer and the tarball alike. The shell activates a COM `IThumbnailProvider`; a PACKAGED one may only run in the shell's surrogate (`desktop2:ThumbnailHandler` + `com:SurrogateServer`), so it is given a STREAM and nothing else -- hence `IInitializeWithStream` only, and ONE class that sniffs FITS (`SIMPLE  =`) vs SER (`LUCAM-RECORDER`) from the first bytes. Built as **`tianwen-thumb.dll`, a NativeAOT shared library with source-generated COM** (`TianWen.Shell.Thumbnails`), which sidesteps the shared-CLR objection to managed shell extensions entirely: its own runtime, no CLR in dllhost, nothing of ours in explorer.exe. The imaging is `ThumbnailRenderer` in TianWen.Lib (debayer, `Downsample` bin FIRST so cost is bounded by the output, the ONE stretch pipeline with `ResolveAuto` moved beside `StretchMode` so the thumbnail matches the viewer), 108 ms for a 3008x3008 RGGB light at 256 px. <br>**The trap that cost the afternoon: an embedded `ILLink.Substitutions.xml` applies only to the assembly that embeds it.** TianWen.Lib carries 57 MB of catalogs against 1.7 MB of code the thumbnail needs, and a consumer-side substitutions file naming them removed nothing (59.6 MB either way; ILC's `ManifestResourceBlockingPolicy` indexes the file by its own module). Shipped as a feature switch IN TianWen.Lib (`TianWen.Lib.EmbeddedCatalogs=false` via `RuntimeHostConfigurationOption Trim=true`), pinned both ways by `EmbeddedCatalogFeatureSwitchTests`: **59.6 MB to 2.81 MB**. Verified through the REAL shell pipeline (`IShellItemImageFactory::GetImage`, `SIIGBF_THUMBNAILONLY`) with a temporary per-user registration: `0x8004B200` with no handler, `S_OK` 256x256 in 197 ms cold with a fresh `dllhost.exe` surrogate, 4 ms on the second ask from the shell's cache. <br>**Caching is the shell's** (`thumbcache_*.db`, re-extracts only on a cache miss or a newer mtime), so the handler is stateless and the library keeps no cache; a future in-app strip should READ `IThumbnailCache`, not build a third. Ships inside the viewer's publish tree (CI copies it beside the exe on the two Windows legs), `build-msix.ps1` checks the CLSID agrees with itself and the DLL is in the tree being packed, `FileAssociationRegistrar` writes the same two keys for the tarball. |
| [meridian-flip-verification](meridian-flip-verification.md) | **P0-P3 DONE (2026-08-30), PR #212.** The session used to confirm a meridian flip on ONE signal -- `PerformMeridianFlipAsync`'s `if (newHourAngle > 0)` -- which on a mount with a COMPUTED pier side (`PointingStateSource.Computed`: LX200 base, SGP, `FakeMountDriver`) cannot fail and cannot detect a flip that did NOT happen: HA is `> 0` the instant the pointing is west of the meridian whether or not the tube flipped. Worse than the LX85 case that prompted this (a goto that misses the firmware's flip window and tracks straight through), the likelier way in turned out to be `FlipAction.AlreadyFlipped`: on such a mount the reported pier side turns over as the POINTING crosses, so the loop concludes the firmware auto-flipped and SKIPS THE SLEW ENTIRELY, then images on with the OTA unflipped and the guider's Dec inverted. N.I.N.A. is equally blind (it re-reads `SideOfPier`, which an LX200 ASCOM driver computes the same way). The witness is the image: a GEM flip is a pure 180 deg field rotation (parity-independent, so mirror count is irrelevant), and the post-flip recentre ALREADY plate-solves a `WCS` with a CD matrix while the last pre-flip solve is already in `_plateSolveHistory` -- no extra exposure, no new state. **Shipped:** `WCS.RotationDeg` (position angle of +Y, `atan2(CD1_2, CD2_2)`, 0 for north-up) + `RotationDeltaDeg` over a new `CoordinateUtils.ConditionDegreesSigned`; `MeridianFlipVerification.FromSolves` (pure, `Flipped`/`NotFlipped`/`Inconclusive`, beside `MeridianFlipDecision`); the verdict OVERRIDES the mount's report only where `PointingStateSource == Computed`, and a field that did not turn now makes the session COMMAND the flip rather than accept the claim. Three rules ride on it: an unreadable witness (missing solve, centre-only WCS, an angle no flip can produce) must fall back to the mount's report or every rig on a coordinates-only solver fails every flip; the pre/post pair must come from ONE camera (sensors sit at different rolls); and the fake had to be made honest first -- `FakeMountDriver` now carries a MECHANICAL tube state that only motion changes (a sync must not touch it, or plate-solve centring silently 'flips' the mount), `FakeCameraDriver` rolls its render off that state and never off the report, and the guide scope's offset became polar and instrument-framed so it turns over with the flip. **P3's sense fix is in:** `WithMeridianFlip(decIsSkyRelative)` rotates the measured axis ANGLES (`CameraAngleRad + PI` always, `DecAngleRad + PI` only for a sky-relative-Dec mount) rather than negating rates, because the angles are the basis `TransformToMountAxes` decomposes against and negating a rate is equivalent only for exactly orthogonal axes. `reverseDecAfterFlip` keeps its meaning (gate: does a detected flip re-orient the calibration) and its `true` default -- only what re-orienting DOES changed, which was never the switch's job; the internal property is renamed `ReorientCalibrationOnFlip` since a name asserting wrong physics is what let the bug live, and the Dec convention is a MOUNT fact (`DecIsSkyRelative`, false everywhere here) rather than a user setting. The old test's premise was the bug: it defined the post-flip rig as RA-unchanged/Dec-inverted and verified the code assuming exactly that; corrected, it fails the old implementation 6.40px -> 69.46px. **`Session.GetSideOfPierAsync` is now the canonical pier side** -- a `Measured` driver believed verbatim, a `Computed` one only until the session knows better, latched on the slewing-to-idle edge in `PollDeviceStatesAsync` (a goto is the one moment the report is certainly right) and moved thereafter only by a verified flip. The built-in guider reads it through a `PointingStateOracle` delegate the session sets at init, so the dependency runs session -> guider and never back. **The E2E runs on the REAL `CatalogPlateSolver` as of 2026-08-31** and reads 180.00 deg of field rotation, gate 80/80. The recorded belief that the solver could not lock a fake field was WRONG: measured by `FakeFieldSolveProbe`, the fake's projection is exact (every detection lands on a catalog star under the true WCS) and ten configurations all solved. The two real causes were both in the harness -- `FakeExternal` THROWS from `GetCelestialObjectDBAsync` unless `CelestialObjectDB` is set, which `FakeCameraDriver` swallows into a RANDOM ~50-star field (the '43 detected' that was misread as density), and 512x512 spans 0.28 deg holding 2-7 Tycho-2 stars against `MinStarsForMatch` 6. `CreateSessionAsync` gained `withCatalogStarField` (shared catalog + ROI 2048); the resolve failure is a WARNING now, and the arrange asserts a catalog field was rendered before asserting any outcome. Gate on 'no rotator moved' once `DeviceType.Rotator` exists. |
| [video-guiding](video-guiding.md) | **NOT STARTED** (written 2026-08-29, user referenced Frank FreeStar8n's MetaGuide). Two distinct, cross-linked-from-nina-parity halves: (1) supporting MetaGuide as an EXTERNAL guider the way `OpenPHD2GuiderDriver` supports PHD2 -- NINA already does this via MetaGuide's `MetaMonitor` UDP broadcast (port 1277, read-only telemetry: star intensity, dither-settle), likely a poor fit for `IGuider`'s command-oriented shape since MetaGuide drives the mount itself; (2) adopting MetaGuide's own technique internally -- confirmed from the actual 2024 manual (PDF, supplied by the user after smallstarspot.com 403'd automated fetches): a peak-windowed "hot spot" centroid per video frame, `AccepFrac`-based lucky-imaging frame-quality selection ("Lucky Guiding"), then a rolling-window average of the accepted frames' centroids (`StackTime`, constrained `<= GuidePeriod`) drives the correction. Precision note logged in the doc: the manual's own example runs corrections "every 1 second" -- the real claimed win is centroid accuracy on a mid-range mount, not necessarily a much higher correction rate than PHD2. Reuses `IVideoCameraDriver.CaptureVideoAsync` and the `IFrameQualityEstimator`/`LuckyImagingStacker` grade-and-keep-best-N% logic already shipped for planetary imaging; the genuinely new pieces are the peak-windowed centroider and the 2-vector rolling-average loop. |
| [nina-parity](nina-parity.md) | **INDEX, not a plan** (written 2026-08-29, raised by the user). Tracks feature parity against N.I.N.A. across every area that already has a `TODO.md`/`docs/todo/*.md`/`docs/plans/*.md` item: sequencing/automation, focus, guiding, plate solving/polar alignment, mount control (incl. the missing Rotator/Dome/SafetyMonitor device types), camera driver coverage, remote/API, image processing. Biggest open gaps ranked there: Dome+Rotator+SafetyMonitor (zero implementation), mount safety limits needing a config UI (mechanism shipped 2026-08-29, nothing wires it up yet), multi-night progress + resume-interrupted-session, PlayerOne/ToupTek/SVBony native drivers, discrete autofocus triggers beyond the HFD trend. Update its status cells in place as linked items ship; don't let it drift from its targets. |
| [wcs-reprojection](wcs-reprojection.md) | **NOT STARTED** (written 2026-08-29, out of the astropy-parity audit). Uses `WCS.PixelToSky`/`SkyToPixel` *generatively* to resample pixels rather than just report a header: P0 single-frame SIP undistort (pull/inverse-warp, pixel<->pixel only, no sky round-trip needed), P1 multi-frame reprojection onto a shared WCS for mosaic stitching (the Montage/SWarp/`reproject` approach -- needs every tile's own solved WCS). Explicitly contrasted with `DrizzleStrategy`/`DrizzleKernel`, confirmed push/forward-splat by its own code (a coverage map doubling as the rejection map, `DrizzleStrategy.cs:271`) -- deliberately the opposite direction, because drizzle combines many incomplete dithered frames with flux conservation where P0/P1 resample one already-complete source. Not common in consumer stacking tools (Siril/DSS/PixInsight's core stacker/N.I.N.A.) because same-pointing stacking doesn't need it; professional mosaicking tools (SWarp, Montage, `reproject`, AstroDrizzle) already use exactly this. |
| [pixinsight-parity](pixinsight-parity.md) | **INDEX, not a plan** (written 2026-08-29, raised by the user). Tracks the pipeline's biggest recurring comparison point by mention count (69 across 22 files) -- SPCC/stretch semantics/GHS DONE, background extraction and narrowband colour NOT STARTED, RC-Astro points to its own plan. Folds in Siril/GraXpert (background-extraction prior art) rather than giving them separate docs. SetiAstro gets its own section with **five** distinct relationships (AI4 model vendor, ported PJSR scripts GHS+StarStretch, Statistical-Stretch-derived features already shipped as Luma mode/LumaBlend/background-neutralization pivot1 but uncross-referenced from `stretch-pipeline.md`, the SASP calibration data feeding SPCC, and SASPro the standalone app -- distinct from the PJSR scripts -- as `background-extraction.md`'s ABE reference implementation) -- do not collapse them. SharpCap deliberately excluded (different tool category, already well-documented at its points of use in polar-alignment.md/live-planetary-capture.md). Top gap (Statistical-Stretch cross-reference in stretch-pipeline.md) fixed 2026-08-29; background-extraction and narrowband-colour scoped the same day (Siril-scripts prior art resolves both plans' open questions, the MathNet.Numerics dependency claim was found false and corrected, narrowband-colour's P1 = Phases 1+2 only, ~1 day, unblocked) -- both remain NOT STARTED but no longer open-ended. |
| [astropy-parity](astropy-parity.md) | **INDEX, not a plan** (written 2026-08-29, raised by the user). Tracks `TianWen.Lib` (the standalone-NuGet library, not the app layers) against Astropy's subpackage structure -- a library-vs-library comparison, not a feature-application one. `astropy.time`/`.coordinates` math is genuinely ERFA-grade (`Astrometry/SOFA/SofaFunctions.cs` + `Transform.cs`) but not exposed as public `Time`/frame types; `astropy.wcs` covers TAN+SIP only (the one projection amateur imaging needs); solar-system ephemeris is DONE and self-contained (VSOP87, no JPL-kernel dependency); catalog access is deliberately offline-baked, not a gap (field use has no internet). Three confirmed real gaps, ranked at the bottom of the doc: no `Quantity`/`Unit` type anywhere (raw `double` + naming convention throughout -- the biggest one), no generic `Table` type, no shared `Model`/`Fitter` abstraction (each fit site -- `Hyperbola`, `PolynomialLeastSquares`, `CometRateSolver` -- reimplements its own least-squares). Update its status cells in place as the code changes; don't let it drift from its targets. |
| [comet-integration](comet-integration.md) | **One `stack --comet --remove-stars` run writes the comet layer, the star layer and the finished COMPOSITE** (branch `feat/comet-aligned-registration`, PR #196, 2026-08-27). **Colour, same day:** three SPCC defects found on the composites (clip level read from a rewrapped `MaxValue`; a many-to-one Tycho-2 matcher; the stacking normaliser anchored on the frame minimum, a x3.7 per-frame gain wander) and fixed; the remaining compressor of star colour is the AHD stack debayer's chroma median, measured and NOT yet changed: see the colour section's "Continue here". A comet is sharp only in a stack registered on the comet, so a finished image needs a star-aligned layer with the body taken OUT and a comet-aligned layer built from per-frame star-REMOVED lights, and the two are combined in linear light by adding the body's model back once at its ephemeris position (`master_<slug>_composite.fits`, plate-solved and SPCC'd like any master; no WCS, no centroid, gain measured from the two layers' sky medians: 0.9993/1.0020/1.0021 on SWAN). The body comes out of the star layer by SUBTRACTING `CometModel` (a crop of the starless comet layer, amplitude fitted per frame) rather than masking, because 10P moves 45 px against an 80 px coma and a mask leaves nothing to stack. **The model's reach is decided per channel from where that channel's profile stops falling**, never from a fraction of the peak on channel 0: SWAN's green peaks 3x red, the red-based 1% floor cut the model at 100 px with green still at 1.4 sigma there, and the wings smeared into a band (ridge 0.39 sigma plus a 0.19 re-emergence at 95-125 px); per-channel asymptote reaches of 210/420/330 px leave the track flat to 0.06 sigma across 0-450 px against a 2.36 sigma control. The body is evaluated at the reference's MID-exposure (1.0 px at 245 px/h and 30 s) and the model centre is sub-pixel. **The amplitude is a per-channel median of d/m over an annulus** (least squares over the core dug a -1.1 sigma bowl on 10P; a pooled amplitude painted SWAN's track red -0.84 / blue +0.36 while the luminance mean read flat), **and the nucleus comes from a comet-aligned median stack of the RAW frames** (`CometRawCore` + `SpliceCore`): the star remover takes it with the stars (10P's peaks at 8656 ADU, 30x the coma at 12 px) and it stayed in the star layer as a +7 sigma line until it did. 10P ends within 0.07 sigma of the floor in every band and channel against a 12.2 sigma control. Per-frame starless plates are cached at `<out>/starless/<slug>/` keyed on `SRCDGST`+`STARMODE`, so an iteration is 3.3 min instead of 13. Pinned by `CometModelTests` + `CometComposeTests`. **`stack --comet [designation]` derives its own rate and needs nothing typed**: the designation comes off the `OBJECT` card, the site off `SITELAT`/`SITELONG`/`SITEELEV`, the window off the exposure epochs, and the rate out of a topocentric Horizons OBSERVER track fitted through the reference frame's WCS (`CometTrackRequest` -> `HorizonsObserverSource` -> `CometRateSolver`). Run against the 10P set it answers **12.64 px/hr**, the hand-derived number below, off a 550 ms reference solve. `--comet-rate dx,dy` is the offline counterpart and the override. **The reference frame had to be plate-solved and nothing solved it before** -- registration is frame-to-frame star-quad matching and never needed to know where the sky was, so the only solve was of the finished master, which is too late for a rate consumed while integrating. **An unknown site is a refusal, not a geocentric fallback**, because a geocentric answer is wrong by 3.4 degrees of heading AND fits a straighter line than the correct track, so no downstream check can catch it; `SITEELEV` is the opposite case and defaults to sea level (worth under a thousandth of a pixel), now round-tripping through `ImageMeta.SiteElevation`. Measured on 135 x 60 s of 10P/Tempel 2 (QHY294C, SV 545): track **10.897, 6.412 px/hr** (44.7 px over 3.538 h, 0.211 px per sub, so no intra-frame trailing), departure from linear 0.185 px worst case, so a single linear rate is right and a quadratic would fit noise. Two facts decide the design. The compose runs in **canvas space after the star solution**, because dither is 88.6 px against a 44.7 px track and a frame-space shift is simply the wrong quantity; `Matrix3x2` is row-vector, so `starSolution * translation` is the correct order and reversing it silently gives the wrong basis. And a **geocentric ephemeris is not good enough** (topo minus geo moves 2.74 px across the run, 25x the residual), so `CometEphemeris.TryGetEquatorialJ2000` cannot drive this; it needs a Horizons OBSERVER ephemeris at the header's site. Polar alignment is irrelevant to that, since diurnal parallax moves the OBSERVER rather than rotating the field. **The frames' headers are now correct** (2026-08-25): `FILTER = 'IDAS LPS-D3'` stamped into the 135 lights AND the 51 flats, and `OBJECT` corrected from the capture-time typo `'10p Temepl'` to `'10P/Tempel 2'`, both via `FitsHeaderEditor` through a new `tianwen dataset tag-object` beside the existing `tag-filter` (186 frames digested before and after: 0 with altered pixel data). Without the FILTER card SPCC integrated its SEDs against QE x CFA alone, i.e. modelled a train with no filter in it; adding it moves the channel throughput from R 0.3550 / G 0.4064 / B 0.2386 to **R 0.2488 / G 0.4562 / B 0.2951**, red losing 30% because the D3's notches (NaI 589, OI 630/636) all sit in the red CFA band. `OBJECT` matters twice over: `LightGroupKey` slugs the master filename from it (every output was being filed as `master_10pTemepl_...`), and it is where the ephemeris work reads the target -- so `CometDesignation.TryParse` was **made lenient about a space-separated name tail**, since `10P Tempel 2` is how a human types a target into a sequence and it did not parse before. Routing was fixed with it, because a leniency that stops at the parser looks fixed and still fails in the search box: `TryGuessCatalogFormat` now probes `IsNumberedShape` on the ORIGINAL input as well as the space-stripped one. **The orbit letter sitting immediately against the number is the whole of what makes this safe** -- `10P Tempel` is a comet and `30 Doradus` is not -- and stripping spaces destroys exactly that, leaving `10PTEMPEL` and `30DORADUS` the same shape, so the probe still refuses a bare letter tail and tests pin both sides. **Outstanding:** `sxt` takes the comet's central condensation out of the comet layer (the sharp part; the core should come from the raws or removal be masked away from the body); the two-mask design where the coverage arithmetic allows it; treat the ephemeris as a SEED and fit the nucleus centroids, which is **the only check that can catch a wrong heading**; and a normalisation guard at the enhancer boundary (four separate out-of-distribution inputs each returned a plausible wrong answer). **Afternoon:** the user's "reflection halo" round SWAN (green half-ring up-right at the 420 px G reach under a hard stretch) was the FIELD'S SLOPE inside the comet model: the crop of the comet-aligned canvas keeps the smeared background's gradient, a scalar pedestal cannot remove it, and the annular profile is zero at the reach by construction so it could not see the dipole (edge sectors +32..+40e-4 up-right vs -19..-27e-4 opposite). `CometModel.RemoveBackgroundPlane` fits a plane per channel beyond the provisional reach and removes its slope (G 0.55 / B 0.63 noise across the reach on SWAN; edge dipole G 29 -> 7e-4, B 21 -> 3.5e-4, R's one-sided tail wedge untouched); pinned by `TheFieldsGradientIsNotPartOfTheModel`. |
| [plate-solver-performance](plate-solver-performance.md) | **A, C', D and C (the quad seed) SHIPPED, B obsolete** as of 2026-09-02; what follows is the history in order. Plan written 2026-08-25, after the solver correctness fix landed in PR #195. AOT `tianwen solve` is 1158 ms against ASTAP's 162 ms on the same frame with the same hint, and a hint buys us only 45 ms, so the gap is structural. **Phase A (cancel the losing parity) shipped 2026-08-31**: measured over all 96 frozen Vela frames the losing parity costs 259.5M hypotheses against the winner's 8.1M -- 97% of the seed's entire cost -- and exactly one parity locks on every frame, never both, which is what makes granting the claim on the first seed clear of chance safe. Three traps the sketch missed: the acceptance gate CONSUMES the loser (it overturns the parity pick when the winner fails the chance test), so an abandoned half is re-run uncancelled in exactly that case; two unguarded claims would cancel each other and fail a solvable frame, so the claim is a single-winner `Interlocked`; and `SolveAttempt` is a `readonly record struct`, so tracking the winner by `ReferenceEquals` compiles and is wrong half the time. The wall-clock saving on a real frame is NOT yet measured -- that needs the benchmark harness, which is still the next step. Half of ours is fixed init (Tycho-2 bulk decode) and 39% is the proximity-match loop: ASTAP does ~110k cheap quad-descriptor comparisons once, we do ~3.2M distance computations per iteration, per parity. Four phases targeting **~260 ms from A-C**: cancel the losing parity (916k wasted hypotheses against 32k on the winner), the Tycho-2 region index (this is `TODO.md` 2C, now with a number attached), quad-descriptor matching reusing the matcher `FrameRegistration` already has, and a star-list cap. Written under one rule: **optimise only where it is provably free of accuracy cost**, because a plate solve here is a MEASUREMENT that the comet ephemeris-to-pixel step, the polar-align vector and mosaic placement all consume quantitatively, where "solved" is ASTAP's whole output. So binning is refused on this class of frame even though ASTAP binned this very field 2x: median FWHM is 2.15 px, already critical, and 2x lands below Nyquist. If it ships at all it is gated on measured FWHM, never ASTAP's frame-height proxy. Parity is cached **per camera, never per rig** (an OAG prism gives the guide camera opposite parity to the main camera on the same rig, and polar-align solves guider frames), cannot be inferred from declared optics (FITS `ROWORDER` flips with no mirror), and stays a hint so a stale value cannot make a solvable frame permanently fail. **Phase B is OBSOLETE and phase C is half dead, both by measurement (2026-08-31), and the "Tycho-2 bulk decode" claim above is wrong:** that decode is 0.3 ms of init and the region index this plan proposed building already exists. Init's real cost was two cross-ref arrays still being lzip-decompressed on every start (275 ms), fixed by a build-time expansion (`ExpandTycho2CrossRefExpanded`), so init went 587 -> 343 ms; the per-record base91 string + `Enum.ToObject` box in the same loops cost 33.7 MB and 11 stop-the-world Gen0 collections, now 3.84 MB and 4, the remainder being exactly the output arrays (PR #215). **Phase C's premise contradicted `StarReferenceTable`'s own doc and the probe (`QuadCatalogFeasibilityProbe`) settled it against the plan:** granted a shared pixel frame -- catalog projected through each frame's own solution, every confound removed in quads' favour -- 73.2% of stars are shared but only **15.8% of quads** (0.73^4 would give 28%), because one interloper re-wires which four stars are mutually nearest; the existing matcher locks 6/24 panels there against the pair seed's 96/96, and a quad match yields quad CENTRES where the CD matrix and SIP need hundreds of sub-pixel STAR correspondences. So quads cannot replace the refinement loop. What survives is **C'**, the half the plan cared about most: matching on the five ratios ALONE (`Dist1` never compared, so no scale assumed) recovers the plate scale to 0.27% from a deliberately 3.9%-wrong prior on **23 of 24** panels at ~122k comparisons per panel, ASTAP's own order. That would let the seed's `MinPairLockScaleTolerance` window go +/-5% -> +/-1%, worth **2.8x fewer hypotheses** (24.9M -> 8.8M over 48 parity attempts) with locks flat at 23-24, and it removes the `FOCALLEN` dependency that put a 130 mm lens entered as its marketed 135 outside the window entirely. Quads settle neither the parity (reflection preserves distances) nor the correspondences. **C' SHIPPED same day** as `QuadScaleRecovery`: the recovery runs ONCE before the parity race (it is parity-blind, so per-attempt would duplicate the work and could disagree with itself), each attempt seeds at the recovered scale with a **+/-0.25%** window, and a miss RETRIES on the header's scale at +/-5% -- which is what makes the narrow window safe, since a stale recovery then costs one pass that is cheap by construction and can never turn a solvable frame into a failure. **The guard is the candidate SPREAD and the candidate COUNT is inverted:** the 23 accurate panels scatter to 0.0004-0.0014 (MAD/median) against the one bad panel's 0.3699, a 264x gap, and that bad panel produced 92 candidates -- MORE than any good one (26-74) -- so a count threshold would have ranked the only untrustworthy recovery the most trustworthy; `IQR/median` fails too (0.2968 and 0.1583 on panels recovering to 0.006%/0.001%) because its percentiles sit in the contaminated tails. A window is a WIDTH and a CENTRE, and sweeping width alone measures the centre: against the DECLARED scale locks hold to +/-0.5% then collapse (10 at 0.25%, 2 at 0.1%) because the declared scale is itself 0.26-0.31% off the solved one, while against a recovered centre all six widths hold 23 locks and hypotheses fall monotonically to **11.1x**. 0.25% rather than 0.1% because the sweep centred on the SOLVED scale while recovery delivers 0.065% worst case; the last 1.5x is what an EXACT prior earns, i.e. a scale carried forward per (OTA, camera) alongside phase A's unshipped parity cache. **The correction applies to the whole attempt, not just the seed** -- the seed transform, the origin `InverseTanProject` derives and the CD matrix all live in one projection's space, and correcting only the seed mixes two, the shape of both bugs the acceptance gate originally found. On the committed NGC 3576 frame it fires and beats the header 2.5x (header 2.8724"/px, recovery 2.8647 from 27 candidates, solve 2.8669). **The negative case is not the obvious one:** asserting an unrelated field yields NO scale is wrong -- 8 of 24 non-overlapping pairs answer and all are right, because both panels share a camera so unrelated sky genuinely shares a plate scale; the real hazard is a confident WRONG ratio, so the test shifts the second field's scale (x0.80 tracks it, x1.25 declines, never invents ~1.0). **Phase D shipped 2026-09-01 (PR #220)**: the star list carried into matching is capped at 500 and detection binning is PROPOSED by the plate scale but VETOED by the measured star width (`MinSampledFwhmPx`). **Phase C came back from the dead and SHIPPED 2026-09-02 as the quad SEED, not as a replacement for the refinement loop.** The "quads do not work against a catalog" verdict above was measuring `StarReferenceTable`'s own matcher: it tests `Dist1` in absolute pixels beside five dimensionless ratios, right for stacking and wrong for a catalog whose projected `Dist1` carries the very scale being recovered. Matching on the ratios alone with a multiplicative `Dist1` window (C1) locks 24 of 24 frozen panels where the mixed-unit test locks 6 (deterministic: the RANSAC is seeded); a hint wrong by 0.70 frames, the wrong parity and a 3.9% wrong scale cost nothing (C2'); and through the PRODUCTION projection on the real panel-10 crop every pool policy locks with the frame centre 0.0-0.1 arcmin from ASTAP's (C3 (a)). `CatalogPlateSolver.TrySeedByQuadMatch` now runs ONCE, in one parity, AHEAD of the parity race, through a rectangle 0.6 frames wider than the hint, and hands the race its origin, its scale and its parity belief (the affine determinant's sign); the pair-lock still seeds at full fidelity from there and the gate still decides. The crop that carried its parent's pointing, 93 arcmin off: 10.85 s -> 0.28 s, 9.2M -> 8,381 seed hypotheses (1,094x); the second solve 9.9 s -> 65 ms. Two rules that bite: the catalog cut is by density over the area the query box actually COVERS of the window (a 9x window cut at 9x inside a 2.25x box locked 0 of 24), and a relocation placed AFTER the race saves nothing by construction -- the first C3 attempt was a tier behind the positional search, which answers in 48 ms; the seconds were the two parities failing at the hint. The cache's end-to-end pin on the crop is gone and correctly so: the seed tells the FIRST solve what the cache used to tell the second. Not done, deliberately: replacing the pair-lock with the quad affine, and retiring `QuadScaleRecovery` now that the seed's ratio supersedes it whenever it locks. |
| [inspector-high-level-telemetry](inspector-high-level-telemetry.md) | **PLANNED (raised by the user 2026-08-22).** The inspector describes the FRAME in exquisite detail and the PROCESS not at all: no working set / GC / heap, no resident-cache counts, no texture bytes or staging high-water, no frames-painted-vs-partial, and no way to ASK for the GPU/font/version facts that are computed once at startup and logged. Decision taken: fold it into the SDL debugger rather than have AppShell join the transport separately -- AppShell owns no window, render thread or lifetime, so its facts are a SECTION of a host's report, not a second endpoint (two endpoints per pid also has no representation in `list_instances`). Generic process half in `DIR.Lib.Diagnostics` so the TUI gets it free, GPU + frame-loop half in SdlVulkan.Renderer, domain half via a callback like `AppState`. Two rules: a stats read must not perturb what it measures (no `GC.Collect` behind a command, nothing allocation-heavy on the render thread the inspector runs on), and counters are cumulative-diffed-over-an-interval like the TUI paint line, not instantaneous. |
| [atlas-planet-detail](atlas-planet-detail.md) | **PLANNED (raised by the user 2026-08-22).** A selected planet's info panel is much poorer than a selected comet's: no vmag sparkline, no visibility curve, and no date for the next opposition / greatest elongation. Two findings move the work: `SkyPathEventDetector` already detects GE / opposition / stations from the sampled path and is already drawn as labelled rings ALONG the arc, so A3 is a text row over existing tested math -- except that a planet's path window is 120 days while Mars oppositions are ~26 months apart, so the event query needs its own long coarse sweep; and `PlanetInfoPanel` fills `VMag` from the STATIC `ICelestialObjectDB` value, which for Mars is off by magnitudes over its synodic cycle and never moves with the time scrub, so the light curve is also the fix for a number already shown wrong. Sequencing A3 -> A1 (sparkline + the live-magnitude fix) -> A2 (visibility curve, one design question open: tonight's altitude vs transit-altitude across the apparition). |
| [viewer-memory-footprint](viewer-memory-footprint.md) | **A/B/C DONE, D3' DONE, D1' SHIPPED for raster-backed documents (written 2026-08-20; the status line here read NOT STARTED long after the phasing table below it said otherwise). HIGH for what remains.** D1's shipped COST is now recorded in the plan: D1' itself cost nothing (a predicted-not-taken bool), while the thread-safe derivation of residency costs +8.7% to +20.3% on the resample loops and is paid for by `Image.ResidentPlanes()` hoisting the resolution out of the loop -- and the first reading MIS-ATTRIBUTED the whole band to D1' because it compared builds differing by two changes. A 13228x9354x3ch 8-bit TIFF costs ~2.5 GB, and ~472 MiB of that never comes back. The budget decomposes exactly at 19 B/px: 3 for the decoded TIFF raster, 12 for the float Image (one float[h,w] per channel), 4 for the Vulkan staging buffer, plus 12 B/px of device memory -- ~3.57 GiB total on a shared-memory GPU. Three independent items. M1: EnsureStagingBuffer is grow-only and freed only on dispose, so one big upload high-water-marks 472 MiB for the process lifetime and every small FITS opened afterwards still carries it; fixed by an explicit TrimStagingBuffer from the document-load path, NOT by freeing inside UploadChannelTexture (the live path uploads a ~104 MB channel per frame, so that shape would churn the hot path). M2: TiffReader.Read returns a complete raster although it already decodes strip by strip, so raster and float planes are both resident during conversion; a decode-into API in SharpAstro.Tiff takes 354 MiB off the peak. M3: the floor is viewer-local, not an Imaging change -- Imaging is rightly float throughout, but the viewer converts 354 MiB of 8-bit samples into 1,416 MiB of floats and then asks the GPU to sample them back to normalised floats, which R8Unorm/R16Unorm does for free with the shader untouched (texture() returns [0,1] either way). M3 is the largest win and the least certain scope: it needs a decision on what AstroImageDocument holds and how Enhance / plate solve obtain floats on demand. |
| [viewer-prerelease-fixes](viewer-prerelease-fixes.md) | **P1-P8 DONE; P9-P10 UNCONFIRMED; P12 + P14 + P16 DONE 2026-08-22; P17 DONE; P11 HALF (version + status shipped, model download open); P18 + P19 DONE 2026-09-04; P13 NEXT RELEASE; P20 (needs the web side) + P21 + P22 BACKLOG; P15 open** -- P17-P21 came from a second band of user notes (2026-08-24/26/27) swept into the plan on 2026-08-27/28: right-click copy (P17, fixed), no Save at all and Open as a word where an icon would do (P18, fixed: `DisplayRasterExport` writes the display raster at the IMAGE's size, and Open/Save are hand-drawn marks), stepping between frames re-solving everything so there is no blink mode (P19, fixed: `DisplayCarry` anchors a folder's run to one frame's statistics -- a DOCUMENT rather than a snapshot, because the anchor's own SPCC triple and star-masked background arrive after the load and a snapshot would leave the anchor looking unlike every frame following it -- with Space blinking the file list), a share link to the web viewer needing `&t=` (P20), and a mosaic's channel views showing the mosaic rather than the debayered planes (P21, where the note's "new `AsChannel*`" resolved to `Channel.AsSpan()` -- no such API exists) (user's notes: `--help` reports no version and the AI-enhancer discovery status is invisible -- with the trap that a status readout must not undo the deliberate deferral of the RC-vs-SAS license probe; gain/ISO + offset are parsed into `ImageMeta` but never rendered in the info pane; no in-depth user documentation for a Store app; and an EMPTY instance does not adopt a file because the gate is folder-scoped by design, fixable through the `PumpInstanceGate` re-bind that already exists). Ten defects found while packaging tianwen-fits for the Store. The three TIFF ones live in `Codecs` and are now released -- **3.9.711** carries P1/P2, **3.10.721** carries P5, and `src/Directory.Packages.props` floats `3.10.*`. P1: SharpAstro.Tiff never inverted TIFF Predictor 2, so 7 of 15 real TIFFs decoded to their own horizontal derivative -- an emboss on smooth data, noise on denoised data, and silent either way (right dimensions, right channel count, no exception). Every affected file used Deflate; every uncompressed one was always fine, which is why test data never caught it. P2 (found while fixing P1): the reader allocated an array + a MemoryStream PER STRIP under a comment asserting strips are large enough for that not to matter -- a ZIP-emitting writer uses RowsPerStrip=1, so the corpus runs 2,009-5,529 strips per image. P5: LZW was unsupported and threw, and its two quirks (MSB-first codes, early-change width growth) each decode plausibly before diverging, so it was validated against a real 16-bit file with an independent implementation rather than only against itself. In this repo: a non-FITS document broke plate solving AND aborted the solver chain (the factory caught only PlateSolverException, so ASTAP never got a turn -- the external path itself was already correct), CMYK rendered as a negative, un-clicking Calibrate did not restore the prior WB, the file list was imperative (no declared regions -> no cursor, no tooltip, invisible to the inspector), and a solver exception put raw binary in the status bar. Registering row regions then broke single-click, in BOTH press dispatchers -- the embedded one and the standalone viewer's own copy in Program.cs -- which is why it took two reports to fix. |
| [tab-strip-unification](tab-strip-unification.md) | **T1-T5 DONE (2026-08-16), T6-T7 REMAIN.** DIR.Lib 8.3 (unpublished) carries `TabItem<T>`/`TabClick<T>`, `TabStripSide`, `TabSizing`, `CanCloseTabs`/`CanReorderTabs`, per-item `IsEnabled`/`Tooltip`, `HoveredIndex`, `IconSize` and `HoverBackground`; TianWen's `RenderSidebar` is DELETED and the rail is a configured `TabBar`. Three of the plan's own calls were wrong: T2+T3 are not separable (content sizing on a vertical strip sets a tab's HEIGHT from its label's WIDTH, so `Uniform` is what makes the side axis usable at all), T4's per-item half belonged in T1 (they are fields of the record), and open question 1 needed NEITHER option -- `TabItem.Icon` is a plain string, because `PixelWidgetBase.DrawText` already routes supplementary-plane codepoints to the emoji face while a rectangle-built `Layout.Content.Icon` could never draw a telescope. Adoption also forced `CompositeWidget<TSurface>` (a child's regions live on the CHILD, so the chrome's five hand-written composition sites had drifted into three orderings plus an omission) -- see [web-host-carve-out](web-host-carve-out.md), which is the same disease one host over. Originally raised 2026-08-15, after DIR.Lib 8.0 made `TabBar` a widget and the question "is TianWen's sidebar in DIR.Lib?" turned out to be "no, and neither is the TUI's"). THREE implementations of "a strip of tabs, one active, click to switch" share no code: DIR.Lib's `TabBar<TSurface>` (308 lines, pixel, imperative, horizontal, keyed on `IReadOnlyList<string>` + an index), TianWen's `VkGuiRenderer.RenderSidebar` (~72 lines, imperative, vertical, emoji-only, hover re-derived from geometry), and `TuiTabBar` (140 lines, cell) -- which **already builds a `Layout` tree**, so the terminal is the one that proves a tab strip is expressible as a tree and the GUI sidebar is the odd one out. Recommendation is ONE description painted by three surfaces rather than a second `VerticalTabBar` widget, on evidence rather than taste: every hand-maintained mirror in this family has diverged in a way nothing caught (the sky-map overlay cache key carried the SAME quantization bug in both copies; `SdlVulkan.Renderer` turned a `DrawTriangles` override into a hide and `WebGl.Renderer` hit the identical class with `PushClip` one release later). Four axes, defaults preserving today byte-for-byte: `TabStripSide` (orientation DERIVES from it, so the two cannot disagree), `TabSizing.Uniform` (a nav rail is square tabs sized from strip thickness -- needed and not in the original sketch, without which a vertical icon strip still sizes itself from label text it never draws), positive-logic affordances (`CanCloseTabs`/`CanReorderTabs`, per-item `IsEnabled`), and `TabItem<T>` replacing `titles`+index, mirroring `DropdownItem<T>` from DIR.Lib 7.29 exactly, which is what kills TianWen's two index-to-enum switches. **Rotated text is the one part that is NOT a flag**: `Renderer.DrawText` has no angle parameter, so vertical text is a new capability across `VkRenderer`/`WebGlRenderer`/`RgbaImageRenderer`, meaningless on a cell surface -- so the vertical strip defaults to UPRIGHT content, inverting the sketch's stated default, which is also what TianWen wants since a rotated emoji is simply wrong. T1-T5 is all TianWen needs (sidebar adopts `TabBar`, `RenderSidebar` deleted); T6-T7 is the prize (strip as a `Layout` tree + a Console.Lib cell painter collapses `TuiTabBar` to supplying items, so one strip serves both surfaces, the Home-board pattern); T8 rotation is separable and probably never happens here. Honest limit: the six-place cost of adding a tab drops to about three, not one. |
| [font-roles-and-icon-baking](font-roles-and-icon-baking.md) | **PARTIAL** -- the BAKE half (B1/B2) shipped, the FONT-ROLES half (F1-F3) and B3 have not (raised by the user 2026-08-19, out of the FITS-viewer toolbar mark work: one common font resolution covering symbols + emoji + OS fallback, plus prebaked icons). **Read the plan's inventory before writing anything**: the emoji half was written TWICE by accident in one afternoon -- inline in `VkGuiRenderer.ResolveFontPath`, then again as `TianWen.UI.Abstractions/EmojiFonts.cs` -- and a third, much larger duplicate (a whole fallback resolver) was one step from being started before `DIR.Lib.FontFallbackResolver` was found. It already does primary -> symbol -> emoji -> per-script with **per-codepoint coverage read from each face's cmap**, `CanRender(Rune)` included, and its own doc carries the reasoning a new design would have had to rediscover (several script faces incidentally carry a few symbols, so the symbol face must precede them or a caret is drawn from a multi-megabyte CJK font). The tell each time was searching for the thing about to be written rather than for the problem it solves. **Five real gaps**: `FontResolver` has no symbol/emoji candidate tables (so the probe got hand-rolled in a host, Windows-only, meaning Linux and macOS resolved no emoji face at all and every emoji mark silently drew nothing); nobody passes `symbolFontPath`, so the ordering `FromRoles` argues for has never been exercised; the FITS viewer does not use the fallback resolver at all, so a codepoint its UI face lacks draws `.notdef`; **existence is not coverage** -- the interim `EmojiFonts.Resolve` checks that a FILE exists, so the Objects mark can resolve `seguiemj.ttf`, conclude it has an emoji face and draw nothing; and a mark at 13-20 design units cannot be reliably hand-drawn (three attempts at a spiral failed -- an outlined ellipse with any core reads as an EYE, and filling it by over-thickening the stroke leaves a hole along the major axis, so it reads as an eye again; only a shape swept from CIRCLES fills), while a colour emoji cannot be tinted and so cannot dim on a disabled button. F1 resolver tables -> F2 delete the stopgap and wire all four roles -> F3 viewer routes `DrawText` through the chain and marks gate on `CanRender` rather than luck: **all three still open**. **B1 + B2 are DONE**, and B1 landed UPSTREAM rather than as the planned `tools/BakeIcons`: DIR.Lib 8.5 carries `IconBaker` (glyph -> coverage runs, **rect runs** consumable by the existing `FillRect`) plus `PixelWidgetBase.DrawCoverageMask`, and the tool over them is the dnx-consumable `DIR.Lib.IconBaker`. It is a LIBRARY API and not only a build step because a build-time bake cannot serve Night mode, whose emoji are not known until they are drawn; the tool ships upstream because nothing in it is app-specific, so leaving it downstream would have produced a vendored copy at the second consumer. The generated DATA stays app-local (`BakedIcons.g.cs`, committed). The planned `TWSH0001`-style staleness warning was deliberately NOT built -- the bake is byte-reproducible (pure-managed rasteriser, verified by re-baking and diffing), so a CI step that RE-BAKES AND COMPARES is strictly stronger and is the outstanding item; a timestamp catches only a forgotten re-bake, not a hand-edited table. B3 (upstream the marks as `IconKind`) is still open. **Licensing gates the bake**: Noto is OFL and already bundled, but `seguiemj.ttf` is proprietary and must never be a bake source even though the runtime probe legitimately falls back to it. Also recorded there: bake resolution is a decision (fixed DPI steps vs one oversampled mask), and **the `sdl-ui-inspector` screenshot is DOWNSCALED ~0.69**, which destroys the sub-pixel detail an icon is made of -- a correctly tapered star arrived as a plain `+` and was judged broken; use a DPI-aware `PrintWindow` capture plus nearest-neighbour magnification. Nothing is blocked on any of it: the toolbar marks shipped with the interim resolver and a geometric fallback. |
| [web-host-carve-out](web-host-carve-out.md) | **NOT STARTED** (written 2026-08-16, out of the tab-strip work). `Pages/Planner.razor` is **2,121 lines -- 65% of the entire web project**, because `WebGl.Renderer` ships a renderer and canvas plumbing and NO host layer: no event loop, no app shell, no window/view, no input mapping, no inspector, all five of which `SdlVulkan.Renderer` and `Console.Lib` do ship. So the page supplies them inline, for one app. Measured contents: 86 key/shortcut references, 53 text-input/focus, 52 render/redraw, 23 JS-interop, 14 pointer/wheel/pinch, 13 composition+hit-dispatch. It reuses nine shared types and references `GuiEventHandlerBase`, `IGuiChrome` and `GuiAppState` **zero** times -- the shared input router exists, the desktop uses it, the web hand-rolls its own, so every rule that router owns is either re-implemented or silently absent. `IGuiChrome` has exactly one implementer. Key correction to the framing: most of the stuck code is browser-SPECIFIC (rAF, `[JSInvokable]`, touch bridging) and belongs in `WebGl.Renderer` beside the canvas, NOT in `.Abstractions`. W1-W2 carve out the host layer, W3-W5 are the payoff (`CompositeWidget`, then `GuiEventHandlerBase`, then `IGuiChrome` gains a second implementer and shrinks to fit), W6 a web inspector is optional. Watch three things: the rAF coalescing rule must survive the move (71% of move-driven repaints were superseded inside their own frame), a trackpad pinch is `ctrl`+`wheel` on a DIFFERENT path from the touch bridge, and W4's behaviour disagreements must be enumerated and decided rather than merged and hoped over. |
| [remote-profile](remote-profile.md) | **P1-P5 DONE (2026-07-27)** -- mirror a rig's `tianwen-server` in the GUI "as if local", and now drive it. The profile lives ON the node; the local side is a **binding record** keyed on LAN.Lib's stable node id (never the address or name, so a DHCP move or a rename cannot orphan it), persisted one file per rig under `AppData/RemoteProfiles`. **P1** LAN.Lib discovery + the profile-switcher dropdown (which also fixed a pre-existing gap: the GUI had no runtime way to switch local profiles at all). **P2** the server session-mirror surface: per-OTA preview, prompt bridging, `GUIDE-STEP`, structured devices, notification ring, telemetry depth, and `POST /session/schedule` -- the one that stops a driven night losing the planner's slot times. **P3** `ISessionTelemetry`/`ISession` split, per-view-context `LiveSessionState`, `TianWen.Hosting.Contracts`, `TianWen.RemoteClient` and `RemoteSessionMirror`, finished with preview JPEGs (opt-in, `X-Frame-Number`-gated), the prompt round-trip, and start/flats/abort driven through the mirror. **P4** binding UX + the physical-presence warning. **P5** served as an **ASCOM Alpaca device plane** on the node rather than the bespoke hub API originally sketched -- the client needed no new code, because `AddAlpaca()` already spoke it, and `RemoteDeviceHub` was never built. <br>**Three findings worth carrying forward.** (1) The overlay model: selecting a rig changes what you LOOK at, never what this node owns, so the local session keeps running underneath and `ProfileSwitchGate` scopes to local rebinds only. (2) Polling is authoritative and events are a latency hint -- which is why the pending prompt rides on `/session/state` and not only on the broadcast: a client that attached after `PROMPT-REQUESTED` fired would otherwise never learn there was a question, and the node would hold the run open forever. (3) Building P5 exposed that device **ownership** was missing everywhere, not just remotely -- see the `device-ownership` row. <br>Two contract bugs the work paid for itself with: 18 wire properties were `required` AND nullable under `WhenWritingNull`, so the server's own JSON was undeserializable; and NaN-is-not-JSON 500'd `/session/state` with no focuser fitted (fixed policy-driven, `JsonNumber.WireAllowsNonFinite` derived from the context's `NumberHandling` so it cannot drift). Plus the nina `prepared-image` encoder had been dividing by `Image.MaxValue` and calling it an auto-stretch, rendering a linear sub near-black; both surfaces now share a stretch-correct `PreviewEncoder`. <br>**The multi-rig dashboard shipped 2026-07-28** as `GuiTab.Home`, the app's landing screen: one card per rig (local node included, titled by the rig with the profile it runs underneath) with an outstanding-prompt badge that carries how long it has been waiting -- which needed three things the remote path did not have, and now does: a prompt's raised-at instant on the wire (never substituted with "when this client noticed"), `GET /api/v1/session/profile` so a node can report which profile it runs at all, and per-mirror poll backoff for a dark rig. The board is read-only w.r.t. hardware, does zero device I/O by where it sits (pre-gate in `PollPreviewTelemetry`, and NOT added to that gate), and renders a published `RigCard` snapshot rather than reaching into the rig registry. **Follow-ups shipped 2026-07-29** (PR #120): the card grew per-target progress / cooling / median HFD / guide RMS / last notification / a meridian-flip countdown (crossing as an INSTANT, `ISessionTelemetry.MeridianFlipUtc`); the board picks its shape (`Auto | Cards | Table`, Auto explains itself when it swaps); and a **TUI home board** renders the same `HomeBoardLayout` tree (`CellMeasureContext.PixelAuthored`) -- the first tree shared across surface kinds, made livable by Console.Lib 4.8's diffing cell buffer. The successor feature (accumulated integration per target) is factored into [multi-night-progress](multi-night-progress.md). <br>**The guide-cam image stream shipped 2026-08-05**, so a remote Guider tab draws the guide camera and its star rather than graphs over an empty panel: `GET /api/v1/preview/guider` on the same `X-Frame-Number` contract as the OTA previews, plus star position/SNR on the state poll, under a separate `PreviewOptions.IncludeGuider` opt-in (the dashboard shows science thumbnails and never a guide frame). Two things it turned up are worth carrying: the blocker recorded against it was already stale (`ISessionTelemetry.LastGuideFrame` existed), while the **actual** hazard was that `ChannelBuffer.AddRef` checked liveness and incremented as two steps and so could resurrect a buffer the camera had recycled -- now a CAS `TryAddRef` under `Image.TryLease`; and the obvious change token (`GuideLoop._guideFrameCount`) counts *corrected* frames past the star-lost `continue`, so it freezes during an outage, which is exactly when an operator wants to watch the guide camera. Still deferred by design (the plan's own scope boundary, not leftovers): hosted polar-alignment/planetary modes, profile editing over the API, the guide star-profile/calibration-overlay arrays, auth/TLS, WAN relay, and full ASCOM conformance so third-party clients (N.I.N.A., SharpCap) could drive a rig. Each is written up with its shape and the reason it was held back in the plan's [Deferred](remote-profile.md#deferred-out-of-scope-for-this-plan) section; **hosted polar alignment is now the remaining likely-next** (user, 2026-07-27) and carries an entry in `TODO.md`. Note when picking any of them up: the device lease is **coordination, not a security boundary** -- it stops a well-behaved second client stealing a rig mid-night, not anyone on the LAN who wants to. |
| [colour-theme](colour-theme.md) | **C0-C2 DONE (2026-08-08), C3-C5 open** (raised by the user 2026-08-07, reviewing colour across the org site, the GUI and the FITS viewer). Three surfaces carry three unrelated palettes: three accents with nothing tying them (site teal `#66ddcc`, `GuiTheme` blue `#88aadd`, `ViewerTheme` cyan `#99ccff`) and three neutral ramps differing in hue bias, the viewer's being pure neutral grey (R=G=B) against its own host's faintly-blue one. Unify on **nine tokens, four states** (System / Light / Dark / Night). Dark and Light are the site's existing pair, already authored and shipped, so the site needs no repaint and the light theme arrives pre-designed. <br>**The real cost is 317 colour literals outside tests** (266 in `TianWen.UI.Abstractions`): both theme records cover only the eight shared `UiPalette` chrome roles and their doc comments say tab-specific colours "deliberately stay local", which was right for one fixed dark theme and is exactly wrong for a second. **And the blast radius leaves this repo**: `DIR.Lib` carries `UiPalette` but also bakes concrete colours of its own with no override seam (`TabBar` holds eight, which are the sibling `pdf-viewer`'s dark palette frozen into a shared widget by its first consumer; `MenuColors` defaults to `VkMenuWidget`'s). `pdf-viewer` already solved the TabBar half in the `drawboard` fork and the shape is the one to port rather than reinvent: a `TabBarColors` record with a `FromPalette(UiPalette)` factory plus a settable `TabBar.Colors`, derived only when the theme MOVES rather than per frame. C1 four-state lookup -> C2 the literal sweep -> C3 `SDL3.SDL.GetSystemTheme()` + title bar + desktop accent -> C4 non-hue reinforcement -> C5 field-validate at the mount. <br>**Candidate cores are rendered** by `tools/theme-mocks.py` into `docs/plans/colour-theme-mocks/`, plus an interactive `studio.html` (edit any token; mock, WCAG table, rod meter and emitted C# all recompute). **CHOSEN 2026-08-07 by the user from the studio: Plate for Light and Dark, Ember for Night.** Dark is therefore `GuiTheme` RETUNED rather than replaced -- same cool-neutral family, but the ramp gains a real blue bias (today's `#1e1e28` has R=G with only blue lifted, reading faintly violet) and every text pair gains contrast (body 10.29 -> 13.78:1, dim 4.66 -> 5.57:1, accent 6.96 -> 9.21:1). **Mixing cores across states is deliberate**: it forfeits Ember's Dark-to-Night continuity, which was that core's whole pitch, on the grounds that continuity is only a virtue if Dark and Night should feel like the same place, and Night must be unmistakable once entered. The site keeps its OWN values and shares only the vocabulary and the four-state model, which is a split not drift (a landing page wants identity, an instrument panel wants neutral surrounds that do not tint the images on top). <br>**The user's eye caught a structural limit the metrics had not surfaced**: Night dim text is unreadable. Not a bad hex -- red on black caps at 5.25:1, so the WHOLE Night text ladder fits between 1:1 and that, body already sits at 4.98:1, and any DimText clearing AA at 4.5:1 is then indistinguishable from body while anything clearly secondary falls under AA. Lifting Ember's dim `#a83500` -> `#b83c00` (3.07 -> 3.57:1) is the entire available margin, so the rule carries the rest: **in Night, anything that must be read uses BodyText**; the Mount card's labels are already separated from their values by position and do not need to be dimmer as well. Same encode-in-form principle as the hue work, extended to luminance. <br>**Night is a dark-adaptation mode, deliberately NOT what System-dark resolves to** (desk hours dominate, where red on black is fatiguing for no benefit). Two findings shape it. **Red is the only cheap channel**: taking V'(lambda) at each sRGB primary's dominant wavelength gives R 0.0155, G 0.49, B 0.61, so blue is marginally WORSE than green and both are 30-40x worse than red. The framing is therefore NOT "avoid blue" -- the popular blue-light advice is circadian melatonin, a different mechanism, and repeating it here would protect the wrong thing by implying green is safe. And **semantic hue survives** inside the long-wavelength band, so of the three severity accents only `SeverityInfo` `#5588cc` must change character. The trap: green carries 71.5% of relative luminance, so a true amber warn out-shouts a red error; dialling warn back to burnt orange `#cc5c00` (0.205) under error `#ff1500` (0.218) restores the ordering while keeping the hues apart. Red on black tops out at **5.25:1**, so AAA is structurally unreachable in Night and the compensation is type size, not a brighter palette. <br>**The image is never recoloured** (user's call, 2026-08-07): astro exposures are faint by nature, so the bright surface at the mount is the chrome. That deletes the GLSL/CPU stretch-mirror work the first sketch carried. **System detection is not in `SdlVulkan.Renderer`** (it carries no theme code at all); the sibling `pdf-viewer` built all four pieces app-locally and three of them we already have, `SDL3-CS` being referenced directly by both GUI hosts. <br>**Shipped 2026-08-07/08** as DIR.Lib 7.11.1921 (C0: `UiPalette` widened to 16 roles as a `sealed record` with computed `IsDark`, plus `TabBarColors.FromPalette` and `MenuColors.FromPalette`) and tianwen PRs #143 (C1: three palettes, `Apply`/`Resolve`, F12) and #144 (C2a/C2b). **Read the phase name sceptically: C2 was "sweep the 317 literals" and the count today is 300**, so what landed is the part that was blocking everything else, not the sweep: 34 frozen `static readonly` palette snapshots plus two style `Default`s made computed, and `VkGuiRenderer` taken from 17 literals to 0. **The frozen snapshots were the whole reason F12 appeared broken and are worse than a bare literal**: a field initialised FROM the palette reads as correct in review, whereas a literal shows up in any grep for colour constants; grep for them loosely, since a pattern anchored on `= GuiTheme.` misses the fully-qualified form that survived the first pass. **Four defects landed and every one was found by running the app or computing a number, none by reading code, two of them introduced within the hour** (a blue online dot from `Success` falling back to `Accent` where Dark's `Info` already was `Accent`; `Connect All` at ~1.4:1; `PlaceholderText` at 1.50-1.89:1 in all three states; Night `Info` at 2.46:1). So **a role lookup makes a colour consistent, not legible; only the PAIRING is legible**, and nothing in the type system checks a pairing. Now pinned by a 16-pair x 3-state contrast matrix in `GuiThemeTests` (floor 4.5, Night 3.4) and by `GuiTheme.InkOn(fill)`, which MEASURES both candidate inks rather than branching on `RGBAColor32.Luminance` (a gamma-encoded NTSC approximation, not the quantity a contrast ratio is defined on, and it guessed the crossover wrong). Two exemptions are tests stating intent rather than gaps: the locked-tab icon at ~1.5:1 (WCAG exempts inactive components) and a selection fill being deliberately subtle at 1.21-1.28 against its panel (lightening Night's drops BodyText-on-Selection from 4.11 to ~2.9). **Three user decisions 2026-08-08 shrink what remains**: the guide graph's RA/Dec keep their hues in Night (neither blue nor green is available and two warm hues 20 degrees apart are not separable at speed, so recolouring would invent a distinction for the rule's sake on the surface most read at a glance); the tab emoji stay full-colour; and **the sky map keeps its own colours entirely, star colours included**. That last one looks wrong until you check WHEN: its background is keyed on SOLAR ALTITUDE, not on the theme, so a 17:00 screenshot shows a full-screen blue-grey chart while at the mount the sun is below -18 degrees and the same function already returns `#020308`, near-black. Nobody dark-adapted sees the blue state, and it spares a GLSL change (star colour is `bvToRgb` in `skymap_star.vert`, so recolouring means a uniform, a SPIR-V re-bake and a CPU/GPU mirror update). **Before judging a Night surface, check whether its colour is a function of something other than the theme and reproduce that state** (`TIANWEN_NOW`). **C2c then finished the sweep**, 300 literals to 98, and the 98 are the point: each is a CATEGORICAL series (schedule target N, camera N, weather class, sky-map object class) where a role lookup would collapse distinct things onto one colour, or the two-trace exemption, and each carries a comment saying which so a later sweep does not "finish the job" by breaking them. **A tint has to stay DERIVED or the sweep just relocates the problem** (a hand-picked "slightly lighter than the panel" is the same frozen literal in a new place), so it added `Mix`, `AltRowBg`, five named button fills and `SkyBand`/`SkyInk` first, and every call site now names an intent rather than a colour. **Two surfaces DEPICT the sky rather than framing it** (the altitude chart's plot, the timeline's twilight bar) and take the opposite treatment from a panel: dark in every state exactly as image data is never recoloured, with the theme changing only the HUE. `SkyBand` is anchored on BLACK, not ContentBg, and that is load-bearing: anchoring on the page inverts the twilight ordering in Light, where further from the ground means darker while on a dark ground it means brighter, so civil twilight would render darker than astronomical in exactly one state (pinned by a test in all three). **The frozen-snapshot bug had a SECOND costume and again only running the app found it**: after the sweep F12 turned everything red except the planner's altitude chart, because `VkPlannerTab` caches it as a GPU texture keyed on the data it draws and a theme switch changes none of that data. `GuiTheme.PaletteGeneration` now bumps whenever the palette moves and the cache key carries it; `Apply`'s existing boolean could not serve, since it tells the caller who asked and a cache is usually held by someone who did not. **Anything caching a projection of the palette owes that generation in its key.** |
| [multi-night-progress](multi-night-progress.md) | **NOT STARTED** (factored out of the dashboard work 2026-07-29). Accumulated integration per target ("M31 - 4.2 h of 12 h - 3 nights") on the home screen, plus scheduler carry-over. The load-bearing decision is already made: the ledger is a headers-only scan of the lights tree (the `MasterGroupKey` grouping `StackingPipeline` already uses), never session counters -- files are durable, counters are not. P0 ledger -> P1 per-pin goals -> P2 scheduler carry-over (optional dependency, byte-identical schedule when absent) -> P3 board section (shared `Layout.Node` tree, GUI+TUI) -> P4 remote rigs (`GET /api/v1/ledger`). The dashboard's rig section was built content-sized specifically to admit P3. |
| [frame-lifecycle](frame-lifecycle.md) | **P0 + P1 + P2 + P4 DONE, P3 PARTIAL, 2026-08-23** (raised by the user 2026-08-06, while adopting the pooled FITS read into the tile-pipelined stacking strategies). No new mechanism is proposed: `ChannelBuffer` refcounts, `Image.Release`, `Image.TryLease` and `Array2DPool` all work. What is missing is a STATED rule for which of them applies to a given `Image`, so ownership is reconstructed per call site (73 `TryReadFitsFile` callers, 203 `.Release()` sites, and no signature says which pairs with which). **Five conventions coexist**: driver-owned/recycled, self-owned (release is a no-op and several callers rely on reading after it), pool-owned, consumed-input (`AdoptImageAsync`, `ScaleFloatValuesToUnitInPlace`), and **identity-or-copy-decided-at-runtime**, which is the one that is not a convention at all: `Calibrator.Apply` opens with `var result = light` and every master is optional, so with no masters it returns its own input, and `if (!ReferenceEquals(x, raw)) raw.Release();` appears at six sites in `SharpenPipeline` plus `Image.Masks`, `MasterPreviewRenderer` and `RawLightDecoder`. That repeated guard IS the missing policy, written longhand, and it fails silently (a wrongly released frame recycles pixels another holder is still reading, so it corrupts a stack rather than throwing). Two costs already paid: the pooled read had to ship **opt-in** purely because ownership across those 73 callers could not be established, and neither tile strategy released its raw frame at all, which was free under self-ownership and became a real leak the moment the read was pooled. P0 name the conventions **(DONE: the policy is the `<remarks>` on `Image`, every producer names its convention and points back, and the two meanings of "released" are separated -- residency is now evict/restore/resident via `TryEvictFloatPlanes`, ownership keeps `Release`)** -> P1 retire the runtime-decided one **(DONE: the measurement REFUTED always-copy -- it adds 7.2 ms and 32 MiB per light and removes no allocation, because the copy's destination is the frame the caller keeps either way. All 14 guards came out by asking the predicate the code already had one branch earlier (`Blend < 1f`, `channels == 1`, `IsNoOp`, an ownership flag); `Calibrator.Apply` had no such predicate so it now CONSUMES its light instead)** -> P2 DEBUG leak detection **(DONE: `ChannelBufferLeakTracker`, a weak-referenced table of unreleased buffers attributed to their producing site by caller info, no finalizer and no strong ref; hooked into `StackingPipeline.RunAsync`'s `[end]`. CI's main leg is Release where it is compiled out, so `test-unit` gained a DEBUG leg filtering `Category=DebugOnly`, guarded by a TRX count because a filter matching nothing exits 0)** -> P3 make pooling the default **(PARTIAL: gap 4 closed -- the pool switch is volatile now -- and master building pooled+released with the pool's return/hit accounting asserted. The rest is blocked on `FrameCache`'s WEAK tier, which structurally cannot hold pooled frames, and on nothing releasing the calibrated frames at all. The DELETION half is withdrawn: the flag never encoded 'ownership is unknowable' but 'this caller will release', which stays a per-caller fact)** -> P4 naming sweep **(DONE: 235 Release() sites audited -- none releases a parameter; `DebayerAsync` was convention 5 on the INPUT side and no longer consumes; `Calibrator.Apply` keeps its name as the one logged exception)**. `Image.TryLease` (2026-08-05) is the first piece named properly: the BORROW primitive, and the thing that had to state that a null buffer array and a released image are different, which is exactly the ambiguity this plan is about. |
| (no plan file) | **device-ownership -- DONE (2026-07-27).** A run now CLAIMS its hardware from `IDeviceHub` (`TryAcquireLease` / `DeviceLeaseSet` / `DeviceOwnershipGate`), and nothing else may disconnect or command a claimed device. Found while designing P5's Alpaca ownership rule, but the gap was **local first**: five UI call sites each guarded on `LiveSessionState.IsRunning` -- three of them a silent `return` with no message -- and `IsRunning` is false during a flat run, which is exactly why `HasActiveRun` exists and none of them used it. So mid-flat-run the focuser could be jogged, the mount pulsed and slewed, and a planetary capture started on the camera being metered. Disconnect was worse: `GetDisconnectSafetyAsync` returns `Safe` for anything that is not a camera, so the mount could be yanked out from under a full session with no warning, after which `ResilientCall` silently reconnected it. A UI flag could never have been right anyway -- the hosted API and the Alpaca plane never see one. Enforcement is asymmetric on purpose: `DisconnectAsync` throws unless `force` (one choke point), actuation asks the gate (no choke point short of proxying every driver onto the imaging hot path). `force` is shutdown-only -- the GUI's "Force Off" means skip the warm-up, which is what the user confirmed; it does not mean kill the night. |
| [automatic-text-input](automatic-text-input.md) | **P1+P2+U6+P3a SHIPPED (2026-08-14); P3b deferred.** `Layout.Builder.TextInput(state, fontSize)` is the whole declaration of a text field, on the pixel AND cell surfaces: the painter draws it, registers its `TextInputHit`, states its I-beam and puts it in Tab order, so none of those can be forgotten. Retired 11 keyed-`Fill`-plus-painter-lambda call sites, the GUI's settable `ActiveTextInput` pointer (now `TextInputFocus`, whose `FocusChanged` the host binds ONCE to SDL Start/StopTextInput -- so the desync bug class is unreachable rather than merely fixed), and the TUI's hand-rolled caret arithmetic. **P4 ran BEFORE the release, against sibling source**, which is what caught two wrong API shapes (`ActiveTab` as an `IPixelWidget` could not serve a terminal; `HandleKey` taking the field beside the focus owner let the two disagree). P3b (declarative `autoFocus`) deferred: it needs a "the dialog opened" signal the library does not have. |
| [serial-probe](serial-probe.md) | **DONE ~90%** |
| [skymap-milkyway](skymap-milkyway.md) | **DONE ~75%** |
| [skymap-gpu-overlays](skymap-gpu-overlays.md) | **PARTIAL ~35%** |
| [tui-live-session-parity](tui-live-session-parity.md) | **PARTIAL ~20%** |
| [first-light-resilience](first-light-resilience.md) | **DONE** (2 of 3 sub-plans shipped; sub-plan 3 deferred) |
| [driver-resilience](driver-resilience.md) | **DONE** (merged to main as 6 PRs + ARCH doc) |
| [fov-obstruction-detection](fov-obstruction-detection.md) | **DONE** (merged to main; scout UI/WebSocket surfacing, single-frame retry, Layer-2 recovery test all shipped) |
| [catalog-binary-format](catalog-binary-format.md) | **DONE** (Option D + Phase 2A + 2B + both halves of 2C; init 729 -> 343 ms) |
| [polar-alignment](polar-alignment.md) | **DONE ~85%** (Phases 1-5 shipped; refraction-corrected apparent pole + live pressure/temperature still pending) |
| [gpu-stretch-tests](gpu-stretch-tests.md) | **DONE ~90%** (Phases 1-4 + follow-ups D & F shipped; Phase 5 separate-CI-job replaced by inline `test-unit` run with mesa-vulkan-drivers) |
| [icc](icc.md) | **DONE ~95%** (Tiff 3.0 + new SharpAstro.Jpeg consumed; display helper + Nina JPEG injection wired) |
| [stacking](stacking.md) | **DONE ~85%** (Phases 1-12 + 8.0-8.3 shipped, selector + 6 strategies + FrameCache + PartialFitsReader; Phase 13 CLI orchestrator + 14/15 LiveStacker wiring + 10 MMF sink still pending). Deep-sky/sidereal only; planetary is the separate [planetary-stacking](planetary-stacking.md) plan  **2026-08 archive/observability wave** (branch `feat/ai-enhancements`): the dataset registrar collapsed onto the stacking core, so registration orchestration exists once (`RegisterLoop`) instead of twice and **the stacker no longer detects stars twice** (a whole load+calibrate+debayer+detect pass per frame saved; the dataset path is structurally unchanged, so its measured baseline stays valid). The pipeline adopted the shared `RegistrationCensus` (a collapsed group's warning is now self-diagnosing, with reference fingerprint + skip causes + spread, in the same shape `SessionRegistrar` prints, so the two paths read side by side) and `StageTimings` (measure/register/integrate/post with Items AND Pixels, so throughput is stored rather than re-derived from logs -- the log-derived baseline got a stage wrong exactly that way; `Register.Pixels` is pinned at 0 because it is the double-detect cost that no longer exists). Dead `Registrator`/`RegistrationSidecar`/`RegistrationResult` deleted: their cache keyed a per-light transform WITHOUT the reference frame, which is unrevivable under the per-group-reference model. Calibration side: bad-pixel map from the lights' own registration UNIONed with the dark map (near-disjoint populations, APP-oracle-validated), calibration epochs (30-day chain gap, time as tie-breaker) so a 2021 dark never blends into a 2026 master, `--require-gain-match` ON by default (a wrong-gain dark is REJECTED, not merely penalised), master provenance (`SWCREATE` + `DATE-BEG`/`DATE-END` + `SNAPSHOT`, `SWMODIFY` on enhance outputs), and `tianwen dataset coverage` reporting per-session calibration coverage through the PRODUCTION matcher (census in [astro-archive-survey](astro-archive-survey.md) section 10). Real-archive interop fixed three silent readers: SBFITSEXT `IMAGETYP` spellings (a MaxIm archive read as `FrameType.None`, i.e. invisible), `BAYERPAT='VALID'` (asserts existence only; the pattern rides on `XBAYROFF`/`YBAYROFF` against ASCOM's canonical RGGB base), and the Bayer-offset spelling itself (reader took only the Atik-legacy `BAYOFFX`/`BAYOFFY`, so **every N.I.N.A. file read at offset (0,0)** -- benign only because (0,0) happens to be true for this archive's sensors). Remaining backlog moved to [../todo/imaging.md](../todo/imaging.md). |
| [planetary-stacking](planetary-stacking.md) | **DONE Phases 1-9 + Bayer drizzle + live viewer + perf** (branch `feat/planetary-stacking`, 2026-06-24): planetary lucky-imaging, separate from deep-sky [stacking](stacking.md) (star-quad align/sigma-clip don't apply to a disk). CPU engine in `TianWen.Lib.Imaging.Planetary` (+ `TianWen.Lib.Stat`): `SerFrameStream` → Laplacian/gradient quality grade → 2D phase-correlation (cached reference FFT) + disk-COM global align → feature-driven alignment points + displacement-mesh warp → per-AP quality-weighted **split-CFA** stack + **Bayer drizzle** (AP-mesh forward-scatter) → 6-level **wavelet sharpen** (default/bandpass/combo presets, `PlanetaryMaster` shared finalize). CLI `tianwen planetary-stack` emits linear + sharpened FITS masters + a high-key planetary PNG. **Live (Phase 9):** `RollingWindowStacker` (frame-capped sliding window, O(pixels) add/evict, align-bound) + `LiveStackPreviewSource` drive a RAW/STACK toggle + Registax-style 6-layer wavelet sliders in `tianwen-fits` (and the GUI viewer tab, shared `ViewerState`); **follow-the-playhead**, off-thread with sharpen-priority + per-request cancellation so slider adjustments are instant regardless of stack time. BenchmarkDotNet + `profile planetary` stage breakdown added (align is ~85-89% of the stack). Validated on a real 30k-frame Jupiter SER. **Remaining:** Meeus-CM derotation (P10/11), live camera stream (P12), GPU compute (P13). |
| [ai-enhancement](ai-enhancement.md) | **DONE Phases 0-7** (CLI side, merged via PR #10) -- model fetch, MTF helpers, ChunkedInference, IStarRemover/IStellarSharpener/INonStellarDeconvolver/IDenoiseEnhancer/IGradientCorrector atomic enhancers, step-based `SharpenPipeline` orchestrator with `SharpenIntermediates` retention selector, ChunkedNafnetRunner shared, GraXpert BGE gradient correction with `--save-gradient`, dual-stretch pipeline (Frank StarStretch + auto-MTF or opt-in GHS + bg-reduce + Reinhard highlight roll-off + Screen recombine + per-plate float TIFFs), `tianwen image {sharpen,remove-stars,flatten,render,stats}` CLI verbs. Plus refinements: 16-px chunk pad, ArrayPool input tensors, `--png` flag, `SubtractiveChromaticNoise` + `Lerp`, `StarsScnrMode` + per-step AI blends, DirectML-on-Adreno for win-arm64 (2.4x speedup), pre-stretched-input auto-detect, `Image.Histogram` median bugfix, `Image.BilinearResize`, `ModelResolver` cross-platform path-separator guard. Phase 7 GUI menu entry + Phases 8-10 (runtime model self-bootstrap, classical fallbacks, NPU INT8 quant) NOT STARTED -- see [ai-enhancement-next.md](ai-enhancement-next.md) for the priority order of follow-on work. **NOTE (2026-06-30):** the CLI-side dual-stretch render + the self-contained `DualStretchPlates` described above were **replaced** by the unified WB-once / per-plate self-stretch / zero-pedestal render in `MasterPreviewRenderer` + `StretchSolver` (both moved into `TianWen.Lib`, CPU-only); the CLI now renders nothing. See [stacking-render-pipeline.md](../architecture/stacking-render-pipeline.md). |
| [ai-enhancement-next](ai-enhancement-next.md) | **NOT STARTED** (Frank-parity Star Stretch port: ColorSaturation + SCNR `preserveLightness`; per-plate stretch via `StretchTransform` abstraction with asinh + GHS + gamma + MTF concrete impls; reroutes the deferred items from ai-enhancement into priority order) |
| [rc-astro-enhancers](rc-astro-enhancers.md) | **Phases 1-3 DONE** (1+2 merged to main via PR #58, branch `feature/rcastro`; Phase 3a-3d on `feat/rc-astro-phase3`, 2026-06-30): RC-Astro CLI-wrapper enhancers (sxt/nxt/bxt via the `rc-astro --json` NDJSON protocol -- encrypted ONNX, so not in-proc ORT), preferred over SETI Astro when present + licensed via the `DeferredEnhancer` proxy (license probe + RC-vs-SAS choice deferred to first `EnhanceAsync`, never at DI build). BlurX-first enhance program when a deblurrer is live. **Phase 3 (options + progress + UI + server):** 3a immutable `EnhanceOptions`/`EnhanceTuning` threaded through `SharpenPipeline.ProcessAsync` + `--ai-backend`/`--bxt-sharpen`/`--nxt-*` on `image sharpen` + `stack --enhance` (no mutable singleton; tuning flags renamed backend-neutral 2026-08-17 to `--deblur-sharpen`/`--denoise-strength`/`--denoise-iterations` when `--ai-backend n2n` landed); 3b per-step `EnhanceProgress` (boundary tick + RC NDJSON sub-step %) -> CLI printer; 3c interactive Enhance action in `tianwen-fits` (toolbar + 'E', right-click cycles backend, off-render-thread, `AdoptImageAsync` swap) -- landed in the FITS viewer since the GUI has no document-viewer tab; 3d `tianwen-server` `POST /api/v1/image/enhance` + `GET .../status` + `ENHANCE-PROGRESS`/`-COMPLETED` WebSocket via `EventBroadcaster`, single-flight `HostedImageEnhancer`, shared `EnhanceOptions.TryParse` (one parser for CLI + server), AOT DTOs registered + win-arm64 publish-smoke-tested. **Deferred nice-to-have:** server job-id/queue model -> [server-enhance-job-model.md](server-enhance-job-model.md). |
| [ghs](ghs.md) | **DONE ~90%** (Phases 0-11 shipped on branch `ghs-converge`: reference math port + B-branch curve + auto-converge `Image.ConvergeGhsStretchFactor`. Subsequent work added mode-target convergence so the bg peak (not the median) lifts to ~0.25 -- median-target left the output dim because median sits above mode for typical astro frames. `--ghs-stages 1|2|3` exposes Cranfield's canonical multi-stage chain (gh-astro 2.7-2.9). CLI refactored to three orthogonal selectors `--star-stretch-mode / --starless-stretch-mode / --stretch-mode` + `--ghs-converge auto|manual`. Post-recombine stretch wired via new `MtfStretchFinalStep` + `GhsStretchFinalStep` (single-pass only on final); `ApplyGhsChain` helper shared between starless + final dispatchers. GHS stays **opt-in** per `feedback_ghs_not_default` -- MTF remains the default starless stretch. Canonical recipe = `--dual-stretch --starless-stretch-mode Ghs --ghs-target Mode --ghs-target-value 0.25 --ghs-stages 3`. {broadband, narrowband, single-light} corpus validation outside SoL drizzle still pending; `--star-stretch-mode mtf|ghs` and multi-stage on `GhsStretchFinalStep` parked.) |
| [narrowband-colour](narrowband-colour.md) | **NOT STARTED** (researched + decided 2026-08-02). An Ha/OIII/SII stack has no colour path at all today: SPCC integrates a Pickles SED over the whole visible, which is the wrong model for a 3 nm passband. Research of three Siril techniques (which turned out to be three different things, not one) produced four ADRs. **ADR-1: ship robust statistical normalization first** (median offset then MAD/percentile gain applied *about the background*, plus an Ha/OIII palette lerp) because it needs no catalog, no plate solve and no new data, is built from primitives we already have, and is what actually fixes red-dominated HOO. It is explicitly **not** calibration and must not be labelled as such. **ADR-2:** the reference implementation (VeraLux Alchemy) is GPL-3.0 against our AGPL-3.0, where vendoring is lawful but reimplementing is still preferred, so reimplement from the recorded maths and source the sensor crosstalk table from DBXtract rather than lifting it. **ADR-3: SPCC narrowband is BLOCKED and the original framing was wrong**: it cannot be done by pointing a narrow passband at Pickles SEDs, because a template is a spectral *type average* that cannot know whether a star shows Ha in absorption or emission over 3 nm; Siril uses Gaia DR3 `xp_sampled` per-star spectra, so this is a Gaia project. Also scoped to exclude SHO on Siril's own guidance (true SHO intensities are green-dominated; the Hubble palette is an artistic remap, not a calibration target). **ADR-4:** hue/chroma editing stays a *render* stage next to `MaskedBoost`, since a luminance range mask degenerates to zero on linear data. **ADR-5/6 came from a fourth video** ("RESCUE the BLUE", 2021, older than the rest): naive HOO is **rank-deficient**; `G` and `B` are the same array, so every OIII region is exactly cyan and no stretch can make blue. The fix is to mix ~15% Ha into blue standing for **H-beta**, which the Balmer decrement ties to Ha (intrinsic ratio 2.86, so Hb ~ 0.35 Ha; dust extinction is what drops the practical number to 15-20%). Our planned mixer already expresses this exactly, so it costs no mechanism, but the coefficients encode a line ratio and therefore ship as **named presets defaulting to a natural blend**, never bare sliders defaulting to naive HOO. ADR-6 records that P2 and P3 **conflict over blue** (P3's weighted OIII estimate discards the blue photosites' independent measurement that P2 would otherwise exploit), so P3 must not chain onto P2 blindly. **ADR-7/8 came from reading the fourth script** (`VeraLux_Curves.py`, initially deferred): it is the same role as AstroColorMixer but with **visible maths**, so it supersedes that investigation entirely, and it gives P5 a concrete shape (value-domain luminance masks, then `L`/`S`/`C`, with chroma as a hue-preserving scale of Lab `a`,`b`; four lines). ADR-7 records the opposite of the obvious conclusion: its Akima spline is **not** an upgrade over our `FritschCarlsonSpline`, which is monotone-*guaranteeing* where Akima merely reduces oscillation, and a non-monotone tone curve inverts local contrast. Do not "modernise" us to Akima. Our 33-knot LUT vs their 65536 is a deliberate std140 GPU-packing constraint, widened on both paths or neither. **The H-beta rationale turned out to be filter-dependent** (user, 2026-08-02), which amended ADR-5's default: an L-eNhance or quad-band *passes* Hb so its blue channel already contains it and adding Ha double-counts, while an Antlia 3nm or L-eXtreme *blocks* it so the blend invents a line that was never measured. On Hb-passing filters the "OIII" channel is literally OIII+Hb, which also means P3's two-line unmixing solves the wrong system there and P4 cannot even describe such a filter (one centre+bandwidth per channel cannot express a channel seeing two lines). Hue rotation has no filter dependency, so it is the only safe blind default; H-beta presets are gated on knowing the filter set, which is profile-level rig config. **ADR-9 then reframed the whole plan** (user, 2026-08-02): the primary goal is a richer picture, not a photometric measurement, and a science mode is additionally in scope, but the two are NOT in tension. One engine, modelled as precisely as the data allows in every mode; the mode gates what may be **added** (invented lines, aesthetic remaps), never how well we model, and anything synthetic is recorded as such in provenance. That inverted the Hb reading: an L-eNhance is not a *contaminated* OIII channel, it is a filter that captured a **third real line**, which solves the rank-2 problem physically (three lines across three channels is exactly determined, so blue is *recovered* rather than synthesised). The three-line solve is therefore promoted to P3a as the largest source of real colour, not deferred as pedantry; only the four-line quad-band case stays deferred (underdetermined). Prioritisation is by **signal recovered per unit of work**, which is why the purely statistical P1 leads (it fixes the actual complaint) and the most rigorous item, P4, sits at the back with almost no aesthetic payoff. **P0 continuum subtraction was found entirely unmodelled** (user, 2026-08-02; zero occurrences of "continuum" in the repo) and slots in ahead of everything: a 3nm filter passes a 3nm slice of *everything*, so an "Ha" frame is really `Ha + continuum` and every downstream statistic is fitted to that sum. Fix is `Ha - k*(R - median(R))` against a matched broadband frame, and the good way to get `k` is photometric: fit narrowband flux vs broadband flux over detected stars, **the slope IS k** since it is the ratio that cancels stellar images. **We already own most of that** (`FindStarsAsync`, quad-match registration, and the flux-fitting shape `Tycho2ColorCalibration` uses for SPCC), so the rigorous version is the cheap one here; the actual new work is pairing a narrowband `MasterGroupKey` with its broadband counterpart. ADR-10 keeps it OUT of the `IStarRemover` role: it removes stars *physically* rather than by inpainting, which is more honest, but it needs a broadband frame and leaves emission-line stars behind, so the two are not interchangeable. **ADR-11: masks may be sourced from a LINE IMAGE, and gate the colour-adjustment stage** (user screenshot of the 2021 video, 2026-08-02). The reference builds a `RangeSelection` mask **from the OIII line image itself** (observed: lower 0.24, fuzziness 0.00, smoothness 35.5, i.e. a hard threshold plus heavy spatial blur to get a structural envelope rather than a noisy per-pixel selection) and uses it to gate a **CurvesTransformation applied after a global mix** (target reads `Modified - Masked`; red pulled below the diagonal inside the mask, so OIII regions go blue while Ha regions keep their colour). So the shape is one global palette decision then one spatially-gated colour adjustment: P2 stays global, P5 gains the mask. The capability gap is that `LuminanceRangeMask` derives from *composite luminance*, which answers "is this bright" (bright Ha included), where the OIII channel answers "is there OIII here", which is the actual question. Strong pushes become safe because they only apply where the signal is: ADR-9 expressed as a mask instead of a model. The hand-tuned threshold is the one manual step and we can **automate it**: it is asking "is this above the noise", which `Image.Background()`'s clipped noise estimate already answers. Phases compound here too, since a threshold on a continuum-contaminated frame selects anything bright (stars included) while after P0 it is a true "is there OIII here" test. **ADR-12: the native input is a set of mono line planes** (`Ha`/`OIII`/`SII`/`Hb`), not an RGB image (user, 2026-08-02), which is how the reference actually works, its PixelMath operating on two grayscale windows `H` and `O`. Two hardware paths reach that state: **mono + filter wheel separates the lines OPTICALLY** (exact, no model, no crosstalk) while **OSC dual-band separates them ALGEBRAICALLY** (an estimate). So P3 is not a core phase, it is the OSC on-ramp, and a mono imager skips it and gets a strictly better result. Two things fall out: P1 restates as "align each weak plane to the reference plane" (Alchemy's "align G and B to R" is that with N fixed at 3), and **SII inverts**: it was never the hard part, four-from-three was; a mono imager with an SII filter just has a fourth plane and full SHO works with no unmixing at all. **Five transcripts + the Siril script repo (2026-08-02) grew this to SEVEN techniques and corrected two attributions.** The "Perfect Narrowband Colors" video is **NarrowbandNormalization** (Blanshan/Cranfield, ported by Cuiv), **not** SPCC, and it is the answer ADR-3 left missing for SHO: renormalize channels so Ha stops swamping green, instead of deleting green with SCNR. And AstroColorMixer's model is **not** locked in the app; the Siril port is public GPL-3.0, which superseded ADR-8: P5 now takes *selection* from ACM (hue-band masks, range masks, low-saturation/shadow/highlight protection ramps) and *adjustment* from VeraLux (Lab L/C, not ACM's HSL). ACM's band labels ("Red / H-alpha", "Cyan / OIII") reveal it uses **hue as a proxy for line identity** because after mixing that is all it has; we keep the line planes, so we select on ground truth. Also: `ContinuumSubtraction.py` gives a **better P0 scale solver than the star-photometry one**: minimise AAD of `nb - (co - median(co))*k`, coarse+fine sweep then an analytic smooth-V vertex fit, no star detection at all (correct subtraction = flattest residual). New **P6 narrowband star colour** (`NB_2_RGB`) fills a gap flagged twice: NB stars calibrate magenta, so synthesize them from the planes (`G = 0.3*Ha + 0.7*OIII`, the same 0.7/0.3 the RESCUE video used, independently). Nearly every phase now has a readable reference in `free-astro/siril-scripts`, including **DBXtract.py at its actual source**, which is what ADR-2 requires. **Validation vehicle is the `D:\Astro-Pics` dataset sweep** (user, 2026-08-02), which also surfaced a real blocker, **since fixed (2026-08-02)**: `SessionDiscovery` keyed sessions without the filter, so a mono Ha+OIII night became one session, the star-count gate rejected the OIII frames and the registrar stacked both filters into one meaningless master. Fixing it disproved two assumptions recorded here. Keying on the canonical `Filter.Name` (what `MasterGroupKey` compares on) would NOT have worked, because `Filter.FromName` is anchored and every descriptive header string (`Ha 3nm`, `Antlia ALP-T`) canonicalises to one `Filter.Unknown`; the key falls back to raw header text. And by the same route the `Bandpass` bit test below is **not** free on its own: `FILTCLAS` is a TianWen-written card, so a N.I.N.A. frame's bandpass comes from that same anchored parse and a 3 nm Ha frame reads `None`. **But the resolver is mostly already shipped** (user, 2026-08-03, correcting a draft that called for a new name parser without checking): `FilterCurveDatabase` embeds 176 spectral transmission curves with `TryMatchFilter` fuzzy name matching, so a bandpass is **measured** by interpolating throughput at 4861/5007/6563/6717 A rather than looked up. Coverage inverts the plan's assumption: the **OSC dual-band case is covered pre-convolved with the CFA** (`SONY_CMOS_{R,G,B}-UVIRCUT_/_<filter>` and Canon equivalents for L-eNhance, L-eXtreme, Antlia ALP-T, Antlia Triband), which is per-channel sensor-times-filter throughput, exactly what P3 unmixing needs and possibly enough to retire ADR-2's crosstalk-table question; **mono narrowband has zero curves**, so the widened name parse is still needed but only for the smaller half. Measuring the curves settled ADR-5 empirically (L-eNhance passes Hb at 64-67% via one wide 4820-5100 A band; L-eXtreme and ALP-T cut at 4960-4980 and block it) and **falsified the plan's `tri-band = Ha|Hb|OIII` claim**: Antlia Triband's three bands are blue 4260-4480 (Hgamma, not Hb), OIII, and a wide 6500-6820 red that passes SII at 74% continuously with Ha, so those two lines are not separable on it. A category name predicts nothing; derive the bits from the curve. The session key deliberately does **not** resolve through the database: grouping asks whether two frames share a filter, which raw text answers synchronously, and fuzzy-matching would let two different filters collapse into one session. An audit of the archive's distinct `FILTER` values is still the sweep's first output. Good news is how much already exists: `MasterGroupKey` carries `FilterName` + a **`Bandpass` bit-flags enum with Ha/Hb/OIII/SII members**, so narrowband dispatch needs no new metadata and ADR-5's does-this-filter-pass-Hb question is one bit test rather than a new profile field; and `LightGroupKey(MasterGroupKey, ObjectName)` is nearly P0's narrowband<->broadband pairing key already. Best verification prize: **measure our own crosstalk coefficients from the archive** rather than sourcing DBXtract's table, which retires the ADR-2 licence question and grounds P3 in this rig. **ADR-13 pins the mixing domain**: the reference stretches each plane then mixes, but once two planes have been through independent non-linear stretches, `0.7*O + 0.3*H` no longer mixes 70/30 of anything physical; we mix LINEAR (P1 does the make-commensurate job the per-plane stretch was doing, deliberately), so the published coefficients are starting points, not calibrated values. P0 continuum subtraction (conditional) -> P1-2 normalization + mixer/presets (universal, so built first) -> P3 unmixing (3a three-line = real blue) -> P4 SPCC narrowband (blocked) -> stretch -> P5 masked colour -> P6 star-colour recombination. |
| [background-extraction](background-extraction.md) | **NOT STARTED** (design captured; classical poly + optional RBF gradient removal, ports SAS Pro `abe.py`; placed pre-stretch / pre-AI in the linear domain). **Scoped 2026-08-29**: two corrections (the `MathNet.Numerics` "already a dependency" claim was false; SAS Pro's reference checkout is a sibling repo at `~/source/repos/other/setiastrosuitepro`, confirmed GPL-3.0, not the nonexistent in-repo path previously cited) and two open questions resolved with concrete Siril script references (`AutoBGE.py` for sample-placement, `GraXpert-AI.py` for the GraXpert-interop question), both from `free-astro/siril-scripts`. |
| [moon-avoidance](moon-avoidance.md) | **DONE** (branch `feat/moon-avoidance`, 2026-06-12): per-bin Moon penalty in `ScoreTarget` via shared `MoonGrid` - illumination x quadratic proximity, Moon-below-horizon gate; radius is an optional param on Schedule/TonightsBest/ScoreTarget (default 30, ON, 0 disables). Dropped the planned `SessionConfiguration` knobs - the session never scores (planner-only path) so they would have been dead code. 6 tests in `MoonAvoidanceTests.cs`; full unit (2596) + functional (286) suites green |
| [scheduled-starts](scheduled-starts.md) | **DONE** (branch `feat/top-5-todo`, 2026-06-12): `WaitForScheduledStartAsync` + `ScheduledStartOutcome` in `Session.Timing.cs`, called at top of `ObservationLoopAsync`; waits until `Start - ScheduledStartLeadTime` (new `SessionConfiguration` knob, default 3 min) on the mount clock. Same-/past-Start short-circuits (hosted API + legacy + existing tests unchanged), beyond-session-end skips cleanly, late starts proceed unclamped, cancellation unwinds via OCE. 4 tests in `SessionObservationLoopTests` (branch outcomes, real wait with frame-timestamp gap, session-end skip, cancel-during-wait); full unit (2596) + functional (290) green |
| [site-conditions](site-conditions.md) | **DONE** (branch `feat/top-5-todo`, 2026-06-12): pure `SiteConditions.Resolve(IWeatherDriver?)` (live weather -> standard, per value) + `ApplyTo(Transform)`; `IMountDriver.TryGetTransformAsync(SiteConditions, ct)` overload drops the 1010/10 hardcode (standard tier auto-derives pressure from elevation -> retires the `IMountDriver.cs:344/345` TODOs); `Session.ResolveSiteConditions()` reads the live weather driver and feeds 4 session call sites; polar-align swapped to the resolver. **Design correction:** the proposed profile-stored pressure/temp override was dropped -- they are varying values (unlike static lat/long/elevation) so they are never persisted; the no-weather fallback derives pressure from the profile's static elevation. `SiteConditionsTests` (7 unit tests) |
| [skymap-time-scrub](skymap-time-scrub.md) | **DONE** (branch `feat/top-5-todo`, 2026-06-12): `SkyMapState.TimeOffset` stacks on the base instant in the single `viewingTime` derivation in `SkyMapTab.Render`, so it drives sky color / LST / planets / horizon downstream with no rendering refactor. `SkyMapTab.HandleKey` re-points the arrows at the offset (Up/Down +-1h, Shift +-10m; Left/Right -+1d; PageUp/PageDown +-1w), adds `N` (`ComputeMidnightOffset`), `0` (offset reset), `T` (full reset) - redraw-only, never planner recompute. HUD offset chip via `SkyMapState.FormatOffset`; inspector exposes `mapTimeOffset`. 20 unit tests (`SkyMapTimeScrubTests`: FormatOffset, ComputeMidnightOffset, day->night sun-altitude propagation); verified live (inspector): Up x3 / `N` / `0`. Mount reticle stays wall-clock |
| [layout-engine](layout-engine.md) | **DONE -- Phases 0-3 merged to main (PR #34) + released; Phase 4 (single-panel tree) on the Session config form** (2026-06-19). **Theme + engine + shared widgets** (DIR.Lib 5.0): `UiTheme`/`UiPalette`/`UiMetrics`; `LayoutNode` tree (Stack/Dock/Grid/Overlay/Leaf + Fixed/Auto/Star) + `LayoutEngine.Measure`/`Arrange`; pixel painter (`ArrangeLayout`/`PaintLayout`/`RenderLayout` on `PixelWidgetBase`, auto-`RegisterClickable`) + Console.Lib `CellLayout` cell painter over the same arranged tree; surface-agnostic `PixelMenuWidget`/`MenuWidget` (first widget rendering one BuildTree on BOTH GPU + TUI). **Per-row tab adoption** via one shared `FormRowLayout` builder set: Planner target rows, Equipment (all 14 separate `RegisterClickable` pairs -> `InsetPillButton`/leaves + data-driven per-OTA `EquipmentPanelLayout` / section-driver, TODO.md:57), LiveSession (ModePill + steppers + polar config rows + toggles), Notifications Clear button -- all draw==hit. **Phase 4 (single-panel tree):** `SessionConfigLayout.Build` emits the whole Session config form as ONE `LayoutNode` tree (section headers + field rows + per-kind controls), arranged once into a scroll-offset bounds, off-panel nodes filtered, painted inside `PushClip` -- the `cursor += itemH` walk + header clip-hack are gone. 9 `SessionConfigLayoutTests` + 14 `EquipmentPanelLayoutTests`; inspector-verified geometry. **Released chain:** DIR.Lib 5.0 -> SdlVulkan.Renderer 6.4 + Console.Lib 3.0 on NuGet; TianWen pins bumped + merged. **Remaining/deferred:** LALR.CC layout DSL (the *tree* -- execution-order Phase 4 -- is done; the DSL front-end stays gated until the C# builder proves verbose); FitsViewer panel `LayoutNode` port + TUI consumption of the section list judged NOT warranted. |
| [rotator](rotator.md) | **NOT STARTED** (per-OTA field rotation; new `DeviceType.Rotator` + `IRotatorDriver` + ASCOM `IRotatorV4` / Alpaca / Fake drivers; mirrors the `CoverCalibrator` device-type wiring; novel logic is framing-on-center + post-flip re-rotate fanned out over `Setup.Telescopes`. Validates against `FakeRotatorDriver` + ASCOM/Alpaca simulators; real-hardware check deferred -- no rotator at hand) |
| [signal-handler-boilerplate](signal-handler-boilerplate.md) | **DONE -- Phases 1-3** (branch `refactor/signal-handler-boilerplate`; drafted 2026-07-03 after the two route-only passes PR #68 + #70). Three private instance helpers on `AppSignalHandler` -- `Notify(severity, message)` (all AppendNotification + redraw sites), guard helpers (`EnsureSessionIdle` / `TryGetConnected<T>` / `TryResolveIdleOtaFocuser`), and `RunTracked` over the upstreamed `DIR.Lib.BackgroundTaskTracker.RunGuarded` -- swept across the handlers and extended to the ones added since the draft (Flats mode, manual cover, comets). Bespoke error sites (Recompute, SkyMap slew/solve, DiscoverDevices, async device connects, StartPolarAlignment) deliberately left untouched. **Phase 5 shipped too**: the ctor's subscription groups split verbatim into per-concern `Subscribe*` methods across `.Planner/.SkyMap/.Equipment/.LiveSession/.Polar/.Flats.cs` partials (field-alias preambles keep bodies verbatim; ctor call order preserves SignalBus registration order); core file 2566 -> ~860 lines. **Dropped:** Tier 4 `Wire<T>` (by its own kill criterion, three cooler handlers already need three shapes). NOT a fluent DSL / MediatR / source generator (SignalBus stays deliberately lightweight). |
| [obstruction-first-light-oracle](obstruction-first-light-oracle.md) | **A + C DONE** (branch `feat/top-5-todo`, 2026-06-13; B follow-up): pivoted from catalog-floor-only to a **zenith calibration anchor** - the rough-focus zenith frame calibrates `detected/catalog-predicted` = transparency x detection efficiency. **A** (oracle): first scout vs `catalog(target, scout-limit, airmass-dimmed) x zenithEfficiency x OracleFactor`, shortfall -> existing `NudgeTestAsync`; catalog-floor fallback + narrowband/sparse/missing-DB guards. **C** (cloud gate): crushed zenith efficiency -> hold-and-re-gauge after rough focus. New `StarDetectionModel` / `CatalogStarCounter` / `NightSkyGauge` / `Session.Imaging.SkyGauge.cs`; 4 `SessionConfiguration` knobs; scout `maxStars` 200->1000. 16 unit + 2 functional tests (existing 11 scout + 41 full-session still green). **B** (transparency HUD from `EffectiveLimitMag`) deferred |
| [multi-source-previewer](multi-source-previewer.md) | **Phases 1-7 DONE** (2026-06-23): fold the standalone SER.Viewer prototype into `tianwen-fits` as a multi-source previewer (FITS+TIFF+SER, auto-switch to playback for SER). **P1-3** (merged, PR #52): SER.Lib wired as a sibling + pure `SerImageBridge` (SerColorId→SensorType+offset, raw→[0,1] into reused buffers); `IPreviewSource` (renderer previews it, `AstroImageDocument` the 1-frame impl, `SerPreviewSource` the N-frame impl, stats/WB computed once, per-frame = cheap raw upload); `.ser` open + auto-switch. **P3.5-6 + auto-WB** (branch `feat/multi-source-previewer-playback`): linear-default + lazy-trailer + cancel/supersede off-thread loads; off-thread frame-paced decode-ahead playback + transport bar; MHC debayer branch in `VkFitsImagePipeline` (CPU/GPU mirror); manual R/G/B WB sliders + gray-world Auto button (shared `ViewerState` → GPU WhiteBalance, also lights up the GUI viewer tab) with the two linear-mode/stat-scale pipeline fixes. **P7** (SER.Lib PR #2, awaiting rebase-merge): standalone `SER.Viewer` deleted; SER.Lib = library + tests (zero-dep, no version bump). Follow-up: a live astro-camera video stream is just another `IPreviewSource`. |
| [altaz-mount-support](altaz-mount-support.md) | **Phase 0 DONE: Phases 1-3 NOT STARTED** (2026-06-22, PRs #47 + #48): SkyWatcher AZ-GTi-class dual-mode mounts. **Phase 0 (safe):** user-selectable `?alignment=GermanPolar\|Polar\|AltAz` setting (default GermanPolar; AZ/EQ is *not* protocol-detectable, so it can't be auto-detected); `AltAz` is **report-only**; `GetAlignmentAsync` returns it + pier reads return `Unknown`, but coordinate slew/track/sync are **refused** (`EnsureEquatorialAlignment` throws) so an alt-az mount can never silently mis-point; the session's meridian-flip gate is now **GEM-only** (`Session.Imaging.cs` reads `AlignmentMode`, fork/AltAz skip the flip cycle). **Phases 1-3 (usable) NOT STARTED:** Az/Alt↔step transforms, `BeginSlewToTargetAsync` pier-gate bypass, dual-axis tracking, position reads (Phase 1 → visual/EAA/plate-solve); alt-az guiding (Phase 2); long-exposure alt-az **imaging** (Phase 3) is **blocked on rotator support** (field rotation; `IRotatorDriver` doesn't exist, see [rotator](rotator.md) + TODO.md). Recommendation in the plan: pursue Phase 1 only on demand, defer Phase 3 until a derotator lands. |
| [flat-frame-automation](flat-frame-automation.md) | **Phases 1-4 DONE** (P1-3 2026-07-01, **P4 2026-07-09**): automated **panel/calibrator + twilight sky** flats, an **on-demand surface + manual panel**, and a **GUI Flats mode**. **Phase 1**; pure `FlatExposureSolver` (`Imaging/Calibration/`, linear-panel-model: Capture/Adjust/Fail) + `Session.TakeFlatsAsync` (`Session.Flats.cs`): per-OTA close cover → calibrator on → per-filter converge → write `FrameType.Flat` → off; opt-in `TakeFlatsOnSessionEnd` after the observation loop, before `Finalise` warms cameras (setpoint temp). **Phase 2**; pure `SkyFlatExposureSolver` (re-metered per frame: Capture/Adjust/Wait/Stop, wrapping `FlatExposureSolver` with twilight-direction awareness) + `Session.TakeSkyFlatsAsync` (`Session.SkyFlats.cs`): opens covers, coarse solar-altitude window gate (`VSOP87a.Reduce(Sol)`), anti-solar zenith slew (`BeginSlewToZenithAsync`, west@dawn/east@dusk) with **tracking off** so stars average out. `FlatSource=TwilightSky` dispatches **dawn** at the end-of-session hook; `TakeSkyFlatsAtDusk` adds a **dusk** session-start hook (cooled first) as insurance against a clouded dawn. **Phase 3**; `ISession.RunFlatsOnlyAsync` (`Session.FlatsOnDemand.cs`): self-contained connect → cool → capture → finalise **outside** `RunAsync` (connects only the flat-relevant subset via the shared `ConnectTelescopeAsync`, mount only for sky, never the guider; focused `FinaliseFlatsAsync`), behind CLI `tianwen flats` (`FlatsSubCommand`) + `POST /api/v1/session/flats` (`FlatsRequestDto`, AOT-registered; mirrors `/session/start`), source/period via shared `FlatRunParsing`. A **manual** hand-switched panel is a **device** (`ManualCoverDevice`/`ManualCoverDriver`, a degenerate `ICoverDriver` mirroring `ManualFilterWheelDevice`: cover `NotPresent`, calibrator `Ready`-on-demand, no analog brightness), assigned to an OTA cover slot and captured through the **same** calibrator path; no `ManualPanel` source, no session branching; registered via `AddDeviceType` so it round-trips through `TryGetDeviceFromUri` (the manual filter wheel's matching gap is closed too). All paths share `ResolveFilterPositions`/`PrepareFilterForFlatsAsync`/`CaptureFlatFrameAsync`/`WriteFlatToFitsFileAsync`; identical output contract (`Flats/<date>/<filter>/Flat/`, grouped by `MasterGroupKey`, path cosmetic). `SessionPhase.Flats` + `FlatIlluminationSource` (Calibrator/TwilightSky) / `TwilightPeriod` enums; 22 solver + 7 flat-orchestration + 4 manual-cover-driver + 4 fake-discovery tests. **Phase 4**; GUI **`LiveSessionMode.Flats`** on the Live Session tab (mode-pill dropdown, like PolarAlign/Planetary): `FlatsBootstrapper` runs `RunFlatsOnlyAsync` with `ActiveSession` set but not `IsRunning` (keeps preview layout + mode pill; `FlatsCts`-gated); `LiveSessionTab.Flats.cs` side panel (source selector `FlatIlluminationChoice` + per-filter stepper + Start/Cancel, phase+status while running); live preview via `Session.Flats.cs` `PublishFlatPreview` + per-filter `_currentActivity`; equipment 💡 "+ Manual Light Panel" (`AssignManualCoverSignal`). New **session→UI user-prompt channel** (`ISession.PromptRequested` + `SessionPromptEventArgs.Respond` + `Session.RequestUserConfirmationAsync`, headless auto-proceed; GUI `PendingPrompt`/`RenderSessionPrompt`/`RespondSessionPromptSignal`) gated on the new **`ICoverDriver.CanControlBrightness`** (false only for `ManualCoverDriver`); prompts case D (hand-switched panel) only; reused for a future dark-frame "cover the scope" prompt (`CoverStatus.NotPresent`). +3 prompt tests. **Deferred:** sky-flat live thumbnails, the dark-frame flow itself, a per-filter progress bar. |
| [device-simulator-ci](device-simulator-ci.md) | **DONE + LIVE-VALIDATED** (merged #72/#73; follow-up expansion 2026-07-04): on-demand CI tests that drive the real drivers against live simulators. New `TianWen.Lib.Tests.Simulators` project, env-gated via `SimulatorGate` (`TIANWEN_ALPACA_SIM` / `TIANWEN_ASCOM_CI`) so it skips clean with no sim. `AlpacaSimulatorTests` drives the production `AlpacaClient` + drivers against a live OmniSim over HTTP -- management API + the camera **ImageBytes** round-trip + telescope/focuser/filterwheel/covercalibrator/switch + camera gain/offset/readout/sensor metadata; devices resolved via the management API, not UDP. **The suite paid for itself on its first live run: caught 2 real Alpaca driver bugs** (mono camera couldn't connect; filter wheel never populated slots) + a follow-up stub audit (`Gains`/`Offsets` hardcoded empty, `LastExposureDuration` null, `ReadoutMode` a no-op -- all fixed). `AscomDeviceTests` moved here from `.Functional`, re-gated `Debugger.IsAttached` -> `TIANWEN_ASCOM_CI`, now mirrors the Alpaca focuser/filterwheel/covercalibrator coverage. `.github/workflows/simulators.yml`: `workflow_dispatch` (`-f suite=alpaca\|ascom\|both`) **+ a weekly `schedule` running the Alpaca leg only** as an unattended regression guard. Real-time settle waits go through `ITimeProvider` (`SystemTimeProvider.Instance`), not raw `Task.Delay`. |
| [serial-lib](serial-lib.md) | **NOT STARTED** (design captured, 2026-07-04). A `Serial.Lib` sibling repo (Lzip.Lib-style) that owns cross-platform, cancellable, timeout-honouring serial I/O, because .NET `SerialPort` async `BaseStream` reads spuriously abort (`ERROR_OPERATION_ABORTED`) on CH34x bridges and are blocking-on-a-thread underneath anyway (dotnet/runtime#28968). Interim fix shipped in `fix/gemini-flat-panel`: cancellable `ISerialConnection.SynchronousReads` (Task.Run over blocking `ReadByte` + `ReadTimeout` slices). P1 = extract that wrapper; P2 = native P/Invoke backend (Win32 overlapped-done-right / Linux termios); P3 = re-point TianWen. |
| [ascom-com-sta-message-pump](ascom-com-sta-message-pump.md) | **NOT STARTED** (finding + design, 2026-07-04). Vendor ASCOM COM drivers that busy-spin `Application.DoEvents()` on connect (Gemini FlatPanel + Focuser Pro confirmed via decompile; iOptron ×2, QHYFWRS232 suspected) hard-crash a headless connect (`0xC0000409`, no managed exception); they assume a WinForms message pump. Mainstream drivers (OmniSim, ZWO/ASI/PlayerOne/QHYCCD) are clean. Fix: host ASCOM COM calls on a dedicated STA thread running a real message loop. The native Gemini serial driver sidesteps this for the panel. |
| [image-codecs-facade](image-codecs-facade.md) | **NOT STARTED** (design captured, 2026-07-05). Two new StbImageSharp-repo packages; `SharpAstro.Codecs.Abstractions` (`IImageDecoder` static-abstract sniff + `IDecodedImage`/`RasterImage` common type) + `SharpAstro.Codecs` (facade: explicit AOT registry + magic-byte dispatch), so consumers reference one package (killing the family version-skew) and sniff/dispatch/`ToRgba8` live in one place instead of three ad-hoc copies (`Console.Lib/ImageDecoder`, `PngImage.ToRgba8`, tianwen `TryReadImageFile`). Name is `Codecs` not `Imaging` (already `TianWen.Lib.Imaging`). Facade must NOT dep DIR.Lib; `RgbaImage` adapter stays consumer-side. P1 abstractions → P2 PNG/JPEG facade → P3 Console.Lib migration → P4 TIFF/EXR/JXR/JXL → P5 tianwen delegates (retire read-path Magick.NET) → P6 FC.SDK. Open: static-abstract vs instance registry; `IDecodedImage` fidelity fields. |
| [comet-ephemeris](comet-ephemeris.md) | **Phases A-D DONE, C2c partial** (branch `feat/comet-ephemeris`, 2026-07-10): JPL comets as a dynamic ephemeris-computed catalog. **A (identity + math):** `Catalog.Comet` (`c` tag) + `ObjectType.Comet`; `CometDesignation` parse/pack/canonical; structured Base91 bit-field `CatalogIndex` (same mechanism as Tycho2/PSR/WDS: kind+year+half-month+order+fragment ≤48 bits), reconstructs from SBDB `prefix`+`pdes`, handles BC years + asteroid-style 2-letter half-months; `IsSolarSystemObject` now covers comets; `CometEphemeris` universal-variable (Stumpff) two-body Kepler propagation (one path for elliptic/parabolic/hyperbolic) + Earth from VSOP87a + light-time + M1/K1 total-magnitude law. Pinned against JPL Horizons: 12P (elliptic) + C/2023 A3 (near-parabolic) reproduce RA/Dec within 1 arcmin at epoch, 12P T-mag matches 15.037. (A first cut used a readable plain-7-bit-ASCII index `cC2024A1`, but the longest real designations, e.g. `C/2001 OG108`, 10 chars, overflow the 9-char ASCII ceiling; the structured bit-pack reaches 100% with `ToCanonical` still round-tripping to `C/2024 A1`.) **B (data layer):** `SbdbCometSource` (keyless bulk `sb-kind=c` fetch, pure `Parse` skip+count) + `SbdbJsonContext` (source-gen, AOT) + `CometRepository` cache `AppData/SmallBodies/comets.json` (weather-pattern TTL 7d + stale-offline fallback, `FetchedUtc` envelope, `ITimeProvider`-driven) + `TryGetPosition`. **Live-validated: 4068/4068 comets mapped** (100%). No separate MPC file (SBDB is MPC-fed). **C-D (DONE, inspector-validated):** sky-map dynamic markers + `com[e]t` (E) toggle + info-panel LIVE alt-az/rise-set + vmag **sparkline**; F3 sky-map search (designation + name); planner `TonightsBest` comet proposals + vmag-driven object bonus; `tianwen-mcp catalog.lookup` comet-aware + observability outlook; selection **paths** (body-appropriate window) + orbital-**event** markers (`SkyPathEventDetector`: station R/D, greatest-elongation, opposition, perihelion). Invariant: comets are NOT in `ICelestialObjectDB` (augment per-consumer). **Remaining:** C2c planner-tab search/autocomplete for comets + a dedicated vmag *chart* (the sky-map sparkline already covers the curve); #26 off-thread path/sparkline sampling. **Deferred:** per-object Horizons ephemeris + non-sidereal tracking rates, bright asteroids (`sb-kind=a`). |
| [soft-discovery](soft-discovery.md) | **PARTIAL** (2026-07-04, `fix/gemini-flat-panel`). DONE: per-source discovery parallelised + non-serial sources overlap the serial probe pass (`IDeviceSource.ConsumesSerialProbe`), probe reads made cancellable-synchronous (fixes the CH34x abort for all serial discovery), boot-aware serial probing (`ISerialProbe.Warmup` + `AssertControlLines`, pass-2-only) so the Gemini panel is auto-discovered; `device discover` ~30 s → ~25 s. NOT STARTED: the GC-style **soft** mode (verify-known-devices cheaply, skip the fan-out when nothing changed) + probe-budget tuning (the ~17 s serial phase) + the pinned-verify DTR-isolation gap + splitting OnStep serial-vs-mDNS discovery. |
| [ai-denoise-deconv](ai-denoise-deconv.md) | **P0 SHIPPED 2026-07-12**, real-archive set built and regenerated (`D:\Astro-Dataset\2025-2026-calgated`, 50 sessions / 135,000 tiles); **P1 SMOKE-VALIDATED 2026-08-12** (eleven local variants on the 1070, §3a; **v8 is the chosen config**: noise-conditioned, nearest-upsample, mixed pairing depth, L2 primary. Noise conditioning is THE fix, so denoising strength has to be an *input*, not a training-time constant. PSNR actively misleads on this data and a fabrication gate is mandatory). **Dataset re-bake DONE 2026-08-13** with Bayer-drizzled masters (gated per session on RGGB + 60 frames + a matched dark; the fix for the per-channel PSF artifact) plus half-master tile pairs (closing the 2.96x training vs 1.41x deployment noise gap): `D:\Astro-Dataset\2025-2026-drizzle`, **64/68 sessions / 197,400 tiles / 64 retained session masters, parity OK**, and it supersedes the calgated set as what P1 trains on. The gate split the set 45 BayerDrizzle / 19 Float16Staged, so a per-channel statistic read across the whole set averages two non-comparable populations; split on `MasterStrategy` first. Its 64 PSF records still carry the pre-per-channel schema and need one `--force-psf` re-measure (cheap: reads the retained masters, no re-registration). TianWen-trained NAFNet denoiser (`IDenoiseEnhancer`) + psf01-conditioned deconvolver (`INonStellarDeconvolver`) as a third backend tier (Auto: RC → TianWen → SAS). **License-clean by construction**: RC-Astro EULA §10 forbids oracle/distillation/paired-dataset use of its outputs, so ground truth is self-derived, Noise2Noise sub↔sub pairs + stack-as-truth eval for denoise, synthetic Moffat-PSF degradation (measured-distribution sweep) for deconv, from the ~20-24k recent (2024+) raw lights on D: (full disk survey, roots, per-era layout, camera table, hazards, captured separately in [astro-archive-survey.md](astro-archive-survey.md)). `tianwen dataset build` exports session-grid fp16 tiles with the *same* MTF/tile code the inference runner uses (zero train/inference skew); PyTorch offline on rented GPU (RunPod 4090, ~$15-50/run), ONNX opset-17 + provenance contract JSON asserted at load; customer machines are inference-only (no Python). Photometric-integrity gates (flux/centroid deltas) as release blockers; the science-safe claim RC/SAS don't make. **Star removal is IN scope as P4** (inject-and-remove bootstrap, section 2.5: synthetic injection gives exact truth by construction, which hand-editing existing stars cannot), and a **P5 ML gradient remover** was added 2026-08-11 (flatten-and-inject; the P0 tiles are the wrong artifact for it, since a gradient is whole-frame and low-frequency while the tiles are 256 px and MTF-stretched). TianWen relicensed to **AGPL-3.0-or-later** on 2026-08-11, so vendoring GPL-3.0 references is now lawful though still not preferred; the RC-Astro restriction is contractual and unaffected. **Revisited 2026-08-20 in the PR #184 session** (the N2N tile-seam fix led back here): the input-rescale probe (`2b4dca99`, `N2nSeamProbe.ReportInputRescaleResponseOnARealMaster`) confirmed sigma conditioning interpolates along the level axis (response peaks exactly at the honest k = 124 band, non-monotone, zero fabrication at every scale) but peaks at only 14/23/40% reduction, so master-depth denoising is a training-distribution problem, not an inference trick. **2.1b SHIPPED 2026-08-29 (`SaveIntermediates`, default OFF)**: the deconvolver's ground truth stays synthetic because the archive has no blurry arm, and that is now MEASURED rather than assumed -- per-sub FWHM p05 1.96 / p50 2.10 / p95 2.55 px, intra-session p90/p10 ratio median **1.04** (2 of 51 sessions reach 1.2, none reach 1.5), the widest real focus excursion (150 steps, ~19 CFZ) moving FWHM only 2.07 -> 2.68 px, and the cross-era "pre-N.I.N.A. is blurrier" idea true but only 2.71 vs 2.45 px median HFD with OVERLAPPING distributions. **And the one source that would have worked is absent: zero auto-focus frames across all 245,213 indexed files**, since both N.I.N.A. and TianWen measured the V-curve and discarded the pixels -- unrecoverable by mining, so the ladders are CAPTURED from now on (9 rungs + the in-focus anchor per run, one field, minutes apart, one sky, one temperature, `FOCUSPOS` on every frame, no exposure cost). Caveat carried forward: a defocus PSF is a disk, not a Moffat, so these validate a synthetically-trained model and train the near-focus rungs -- exactly the top of the `psf01` range 2.2 notes is untrained -- rather than replacing 2.2. **Do not re-propose mining the archive for deblur pairs.** **2.1c added 2026-08-28 (user, NOT MEASURED)**: the *other* half of that note -- targets CROSSING THE ZENITH give same-night, same-field high-vs-low-airmass pairs, which are a real counterpart to 2.6's synthetic flatten-and-inject gradients and therefore worth as much as a CONTROL on that model as a training source; the blur reading of the same pairs is the weak one, because 2.1b's 1.04 intra-session FWHM spread already includes altitude variation, so the first step is to plot FWHM against `AIRMASS` (a card `FitsHeaderEditor` already writes) and see whether a slope exists before building a pairing. Never pair across nights -- transparency, moon and focus move too, which is the confound the zenith framing exists to avoid. (FWHM figures are from the 2026-08-15 store, pre-deblender. Measured 2026-08-29 over 12 sessions through the production `MeasureMasterAsync` path: +4% matched stars, +0.039 px median FWHM, i.e. ~2% -- consistently POSITIVE, the opposite of the naive deblending prediction. The spread conclusion survives; the store still wants a `--force-psf`. See 2.1b.) Section 3b retains the four session conclusions: the half-pair negative is final and properly seeded; the smoke model's inference-time levers are all measured out; pair gradients vanish at deployment depth (sub pairs are the wrong teacher for the master regime); synthetic noise injection carrying the deployed noise SHAPE is promoted to co-primary for the deep end. |
| [smart-framing](smart-framing.md) | **Phases 1-3 DONE, Phase 4 (sky-map group frame) DEFERRED** (2026-07-11): pin M8 with a wide-field profile → the planner auto-groups M20 into the same pointing ("M8 + M20", one scheduled observation at the combined-footprint centroid). Pure Lib core (`FramingGrouper` tangent-plane fit + greedy accretion, NOT quadratic: Dec-band binary search + grid-local neighbour discovery) + profile-persisted sensor specs auto-captured on first camera connect (`OTAData.CameraPixelSizeUm/SensorWidthPx/SensorHeightPx`, offline FOV via `PrimarySensorFovDeg`) + planner wiring (`ComputeFramingGroups` on every pin change, `CollapseForSchedule` at schedule build; no captured FOV → byte-identical schedule). Identity is index-based (cross-indices), no name matching. **Shipped with the SIMBAD merge v4 root-fix**: alias-only identifiers (all Messier numbers) now resolve through the cross-index table in `MergeSimbadRecords` (was: Sh2-25 landed as a standalone "Lagoon Nebula" duplicate because its only NGC-family id is "M 8"); LINQ-free per-record hot path; full 13-catalog SIMBAD + OpenNGC data refresh; both snapshots rebaked (AlgorithmVersion 4); external `lzip` retired for the managed `tools/lzip-util.ps1`. 11 grouper + 5 planner tests; 408-test catalog/framing sweep green; init stays on the snapshot fast path (1.86 s). |
| [web-multithreading](web-multithreading.md) | **NOT STARTED** (research captured, 2026-07-18). Forward-looking; the catalog-load freeze that motivated it is **already solved by AOT** (23 s→0.55 s init, 26 s→0.59 s sweep). Records what real browser parallelism would take. The constraint: real .NET threads (`WasmEnableThreads`) need `SharedArrayBuffer` → COOP/COEP headers → **GitHub Pages can't set them** (verified live). Options: (A) wasm-threads + a `coi-serviceworker` shim (shared heap, `Task.Run`/`BackgroundTaskTracker` parallelize almost free, but bigger download + COEP subresource discipline + Blazor main-thread-affinity marshaling); (B) plain message-passing Web Worker running a **second .NET runtime** (no sharing, header-free, works on Pages today, but ~2× runtime RAM + marshal flat arrays via Transferables + no first-class API); (C) GPU compute (header-free, but needs WebGPU, see web-webgpu). Blazor surface is thin + we're AOT/reflection-clean so dropping it is feasible, but it's not required for any threading path and the `WebGlCanvas`/`CanvasTextOverlay` components are chess-shared → **keep it**. **Recommendation: build none now**; trigger for (A) = real CPU-parallel browser work (in-browser stacking), for (C) = interactive sweep re-scoring. |
| [render-batching](render-batching.md) | **DONE** (draw-call batching, 2026-07-20); `ShapedRunCache` deferred. Trigger: web sky-atlas [O] overlay + zoom-out collapsed the frame rate (~10k `gl.drawArrays`/frame: every extended-object ellipse traced as ~32 unbatched `DrawLine` calls). Root fix needed NO new DIR.Lib API -- `DrawPolyline`/`DrawPolylineDashed` were already `virtual` (their doc says GPU renderers should override); neither backend had. Shipped: **SdlVulkan.Renderer 6.27** + **WebGl.Renderer 1.10** override both to record a whole polyline as ONE draw (Vk: segment quads -> one `DrawTriangles`; WebGl: concatenated stroke verts -> one `Opcode.Draw`, no shader change); **tianwen** `DrawOverlayEllipse` -> one `Renderer.DrawPolyline` + adaptive segments `clamp(radiusPx/2,8,32)`, planner chart pinned-window/moon-dot fills -> `FillRectangles`, altitude curves -> `DrawPolyline`. Guards: WebGl headless command-count test, Vk offscreen pixel-equivalence test, tianwen offline overlay render test. P1 `WritePolylineQuads` dropped (unneeded); `ShapedRunCache` deferred (caching not batching; marginal for the default cheap `AdvanceShaper`; revisit if `ShapingTextShaper` becomes default). P4 (b) horizon cache + (c) projection dedup deferred (CPU micro-opts dwarfed by the draw win). Debug-multiplier side-finding fixed earlier (SdlVulkan.Renderer 6.26 validation opt-in). WebGPU pivot explicitly a non-goal: cost is command count + CPU shaping, identical under WebGPU; the 2.5M-star single-instanced-draw field proves WebGL2 throughput is not the limit. |
| [interaction-primitives](interaction-primitives.md) | **P1+P3+P4+P5 DONE local, 2026-07-21** (all 6 lists on `ListScrollController`, interactive-everywhere pass, `OnPointerInput` host wiring in SdlVulkan 6.28, viewer + preview on `PanZoomController`, sky map on `TapOrDragGesture`); **P2 release chain pending** (+ the P3 web E2E touch test); nothing pushed until the DIR.Lib 6.15 chain ships. Original scope -- Trigger: planner list doesn't scroll with touch/trackpad + the scroll wiring is per-instance MFC-style boilerplate. Audit found the problem systemic: **7 hand-rolled scroll sites, 4 conventions** (row-indexed Planner/Equipment via `ScrollBar`, pixel Notifications/Session, bottom-anchored LiveSession log, FITS FileList -- which STILL has the trackpad truncation bug -- + dead `HitTestFileList`), **8 drag state machines** (incl. a byte-for-byte duplicated cursor-anchored zoom formula in ImageRendererBase vs LiveSessionTab), exactly **1** tap-vs-drag slop (SkyMap `ClickDragThresholdPx=4f`), and a latent pixels-vs-rows unit mismatch on `SessionTabState.ConfigScrollOffset` (GUI writes px, TUI writes rows). Fix: three DIR.Lib 6.15 primitives -- `TapOrDragGesture`, `ListScrollController` (the **"atom"** unit = smallest logical scrollable unit; ONE fractional-atom float subsumes wheel accumulator + sub-row smoothness + drag deltas; host/DPI-independent so the TUI/GUI unit mismatch dissolves; top/bottom anchor; optional interactive/decorative/none scrollbar; tap-on-release row select needing NO host dispatch changes), `PanZoomController` -- then lockstep rebuilds (Console.Lib 3.9 / SdlVulkan 6.28 / WebGl 1.11) and three tianwen migration waves (P3 Planner+Equipment + touch drag-to-scroll + E2E touch test; P4 Notifications/Session/LiveSession/FileList + delete `ScrollBar.cs` + dead code; P5 pan/zoom dedupe). Interim trackpad fix `b3437e02` shipped 2026-07-20, absorbed by this. |
| [controls-upstreaming](controls-upstreaming.md) | **COMPLETE.** U1-U5 SHIPPED 2026-07-23; **U6 (`TextInputInteraction` -> DIR.Lib) SHIPPED 2026-08-14** with the automatic-text-input wave -- same call sites as its focus owner, so one migration rather than two, and it gained the 15 tests it never had (untestable inside a UI project, where its machinery was only assembled by the running app). Promote generic controls from `TianWen.UI.Abstractions` to DIR.Lib per the layering rule (taxonomy: [widgets-and-controls.md](../architecture/widgets-and-controls.md)). U1 `TrackSlider` -> `PixelWidgetBase` protected helpers + U5 `RenderTextInput(RectF32)` (DIR.Lib 6.15/6.16, PR #110); U2 `SearchInteraction<TResult>` base with `PlannerSearchInteraction` + sky-map F3 re-based as subclasses (DIR.Lib 6.18, PR #113); U3 `DropdownMenuState` overflow scrolling (row-snapped decorative `ListScrollController` + `AtomFitEpsilon`) + U4 `ProgressBar` -> `Layout.Builder.Progress` (DIR.Lib 6.19, lockstep Console.Lib 3.12 / SdlVulkan.Renderer 6.32). |
| [layout-driven-ui](layout-driven-ui.md) | **IN PROGRESS 2026-07-21** (L1 done, L2 done, L3/L5 partial, L4 mostly-N/A). ~18 commits converting hand-positioned chrome to `Layout` trees; net code reduction on form-heavy tabs. **L1 GuiderTab** fully layout-driven (one tree + 4 Fill-leaf raster panes; `GuiderTabLayoutTests`). **L2 EquipmentTab** split into 6 concern partials + mount/camera telemetry, cooler controls, device-settings + filter rows, and **`RenderProfilePanel` section walk = ONE `VStack(sections)` tree** (`2a735537`, net -75; `_profilePanelFills` Fill dispatch; live-verified with connected fakes). **L3 LiveSession** mount block + **preview per-OTA panels = ONE `Dock(HStack(columns),Bottom(mount))`** (`3083d559`, net -12; killed the column+row cursor + maxY). **L5 Notifications** rows. Both cursor-stitch orchestrators the endpoint pattern was aimed at are now done. **Gotcha:** a returned multi-row section VStack must be `.WStar()` (Auto collapses to intrinsic). **Refined taxonomy finding:** conversion shrinks *positioning* code only, so form-heavy tabs shrink a lot while viz tabs don't (raster painters dominate + stay pixel); AND a second stay-pixel category surfaced -- **interactive controls with per-element rect coupling** (viewer Toolbar hover + dropdown anchors, drag sliders, transport scrub) resist the DSL because `.Bg` is set pre-arrange. So L4 viewer chrome is mostly intentionally-pixel (controls + histogram); StatusBar already minimal. Original scope below. |
| [web-tycho2](web-tycho2.md) | **COMPLETE (P1 + P3 + P2/P4).** Bring the full ~2.5M-star Tycho-2 catalog to the web sky atlas (was HR-only ~8.6k; `Lightweight=true` strips `tyc2.bin.lz`). A **data-delivery problem, not compute/GPU**. **P1:** lazy-fetch of the whole `tyc2.bin.lz` on first atlas-open, decode+flatten off the first-paint path via the shared `SkyMapState.FillTycho2StarVertices`, render-thread swap over the HR seed. **P3:** IndexedDB cache of the *raw decompressed* catalog (6.67 s cold -> 0.997 s warm). **P2+P4 (2026-08-16):** the catalog is also published as **166 region-aligned lzip members plus a tiny manifest**, so a first open fetches **8.96 MiB of the sky it is looking at instead of an unconditional 28.71 MiB**, and picks up more as you pan. The file was ALREADY segmented by GSC region (9537 regions, a 37.3 KB offset table, fixed 17-byte records) and `Tycho2RaDecIndex` depends on that ordering, so **the record order is unchanged** and `tools/bake-tycho2` derives the members from the committed `.lz`, verifying the concatenation decodes back to identical bytes. **One file per member, NOT byte ranges: ranges are unusable on both hosts** -- GitHub Pages answers `206` over the *gzip* representation (Fastly compresses every content type and `Accept-Encoding` is a forbidden header, so no browser can opt out) and Cloudflare Pages ignores `Range` outright; a `curl` probe hides both. Absent regions are filled with **`0xFF`, the catalog's own no-data sentinel**, so a partial catalog needs no downstream changes at all (zero-fill would paint magnitude -2.0 stars at RA 0h/Dec 0). 256 KB members measured as the knee (+1.4% asset). Two flaws only a browser found: sequential member fetches (50 round trips), and six full rebuilds per pan -- fixed by issuing all requests before awaiting and debouncing the rebuild on the label-settle generation guard. **Bright-prefix side asset measured unnecessary** (the HR seed already gives the instant sky). Retires the wasm-threads/COOP-COEP stack for this plan; Cloudflare Pages remains a pure deploy-time upgrade (verified `_headers` gives immutable caching, and COOP/COEP if threads are ever wanted). |
| [web-webgpu](web-webgpu.md) | **NOT STARTED** (research captured, 2026-07-18). Whether to add a WebGPU backend to `WebGl.Renderer` + reuse shaders + use **GPU compute** to parallelize the sweep without the CPU-thread/`SharedArrayBuffer` wall. Shader intake three-way: Vulkan=SPIR-V (compile), **WebGL2=GLSL source (browser compiles, no toolchain, why the web port was cheap)**, WebGPU=**WGSL only** (needs GLSL→WGSL transpile via naga/Tint, or WGSL twins). No compute shaders exist anywhere today; the tonight's-best sweep is the first plausible consumer (data-parallel, flatten candidates → dispatch → readback; header-free parallelism). Fallback WebGPU→WebGL2 is mandatory (support not universal early-2026) and the `Renderer<TSurface>` seam already supports a second backend (C# opcode layer is GL-agnostic; the JS `flush()`/`syncAtlas()` shim + shaders + `RegisterPipeline` raw-GLSL contract are the WebGL-specific parts). **Recommendation: defer**; trigger = interactive planner re-scoring (time-scrub) or a heavier browser GPU workload. If pursued: bounded branch-only spike (feature-detect `navigator.gpu` + one WGSL compute pass beside the CPU path + prove GLSL→WGSL on one pipeline, then measure). |
| [osc-narrowband-denoiser](osc-narrowband-denoiser.md) | **v17 family COMPLETE (9 checkpoints / 3 arms), two long-running questions CLOSED, one blocking discovery** (2026-08-15). **The training pool is almost entirely OSC NARROWBAND and nothing in the pipeline knows it**: joining the 67 baked sessions against the locked verdicts in `D:\Astro-Organized\_provenance\group-{a,b,c}-locked.csv` gives **39 Optolong L-Ultimate 3 nm + 9 L-Quad Enhance + ZERO broadband** (19 unlabelled, mostly the parked ASI585 group and the 2 poor mono sets), while [ai-denoise-deconv](ai-denoise-deconv.md) records the deployment target as OSC broadband. The bake reads the ORIGINAL untagged `D:\Astro-Pics` roots, so all 67 retained masters carry `FILTER = 'None'` and the 4,724 cards written by `dataset tag-filter` during the reorganisation (in `D:\Astro-Organized`) are invisible to it. <br>**Invention is ZERO and the fabrication metric was measuring its own threshold.** The gate's bar is `med + 5*MAD` *of the array being counted*, so a harder denoiser lowers its own bar and re-detects speckles the input already carried. An absolute-bar twin (`ref_mad` = the INPUT's per-tile MAD) puts all 9 checkpoints at **-19 against a 20.1/tile raw-sub floor**: the models REMOVE 85-97% of the input's spurious detections and add none. The relative count stays a *ranker* (residual speckle against a silky background is a real cosmetic harm) and must never again be read as "invented sources"; the absolute count discriminates nothing, so it is a safety check. Generalises: **a detection count whose threshold is derived from the array being counted measures the threshold.** Invention has a knee at ~0.80x noise and is ~0 above 0.85x, so the deployable operating point is a STRENGTH dial, not a checkpoint property (`n2n_v17c_s0_final.pt` sits at 0.80x, +2.0 relative). <br>**8 sessions beat 60 and capacity is not the lever**: at matched noise on the clean observer session v15 keeps 0.831/0.727 faint amplitude (0.90x/0.85x) against v17b 0.733/0.669 and v17c 0.771/0.690, seed-consistent, with base 48 and base 32 on the same frontier. Whether that is PSF heterogeneity or the 39-vs-9 filter split is **open and must be checked before spending GPU on the PSF hypothesis**. <br>**Red's wide, field-inverted PSF is acquisition plus optics, not a bug, so the P2 hold on it is LIFTED.** Autofocus minimises star size where the flux is (green+blue = 75% of photosites; under a dual-band filter literally the OIII line at 500 nm, which lands in G AND B while Ha 656 reaches only the red 25%), so **red is never what focus was optimised for**: red is the sharpest channel in 6 of 65 sessions, red/green centre width 1.381 quad-band and **1.642 at 3 nm**. A curved chromatic focal surface then makes defocus field-dependent, which is the "inversion". Four code hypotheses falsified (per-channel flux banding, nebulosity, `HalfMaxDiameter`'s `2*rAperture` censoring at 1.2% red vs 1.7% green, Moffat beta at rho -0.10), and the argument that kept it misdiagnosed ("a star cannot be more elongated and sharper at once") is simply false, since defocus and astigmatism are independent. <br>Landed: `069e5b14` field-radius bins from ONE star set matched across channels and banded on a single reference flux (all 67 re-measured via `--force-psf`, `RadiusSampling = "common-stars"`); `c1079f4b` + `ad08d4fc` flat pedestals prefer an exposure-matched dark-flat over a bias, ranked by the error each LEAVES (`\|t_c * 2^(dT/6) - t_f\|` in seconds of the flat's own thermal signal) rather than adding degrees to seconds. <br>**Next, by value:** N1 join the locked filter labels (no re-bake, no heuristic, 48/67 labelled immediately) -> N2 PSF conditioning in the trainer (the v8 noise-conditioning lesson one axis over: make the varying quantity an INPUT rather than narrowing the data) -> N3 restate the deployment target -> N4 ship a checkpoint behind a strength dial -> N5 re-bake from the tagged archive -> N6 mono (deferred; the 2 existing mono sets are poor and must not be a baseline). **Traps re-tripped this session that were already written down: `RGB`/`LUM` are processing modes not filters (a "broadband vs narrowband" pair reported here was really quad-band vs 3 nm), and the whole filter-inference programme was rediscovered as though open.** |
| [filter-inference](filter-inference.md) | **Method VALIDATED on 48 sessions / 7,161 frames, tooling NOT COMMITTED** (2026-08-04). No FITS header in the D: archive names a filter, and folder hints exist on only about a third of paths and actively mislead twice over (`RGB`/`LUM` are *processing modes* on an OSC sensor, worth 4,533 frames of false evidence; mono frames in an OSC archive are narrowband-*extraction outputs*, 130 sessions / 5,942 frames). So the filter comes from the raw undebayered pixels: the CFA samples the passband at three spectral positions, so **background B/G separates layout** (3 nm dual-band `0.5639 +/- 0.0099` over 44 sessions vs L-Quad `1.009-1.082`, i.e. **43 sd**) while sky rate in e-/px/s / airmass corroborates width. R/G overlaps and is useless; two different dual-bands are **not** separable. A 93% moon at +33 deg producing *less* background than a moonless night on the same rig is the control that proves the metric tracks the filter, not the night. <br>**Bias must be MEASURED from the archive's own bias frames keyed on `(gain, offset)`, never fitted per frame**: the fitted version returned gains 0.119-0.344 where truth is 0.197 (and one bias of -7177 ADU), producing four wrong verdicts including a session in a folder named `Vela SNR P2a 240s L-Ultra`; measuring cut within-population scatter **7.4x**. It also killed my own explanation of the data (a reported r=0.913 between B/G and sky rate was both quantities sharing one bias error; with measured bias r=+0.14). One era needs per-channel bias because ZWO WB was on (`649/516/649`, corroborated by folder names recording `78r 63b`). <br>**Committed today: only the write end** (`dataset tag-filter` + `FitsHeaderEditor`, used for all 4,724 cards in the verified 5,750-file / 96.95 GiB reorganisation). Phases **F1** bias-library, **F2** frame-scale detection (the times-four N.I.N.A. scaling), **F3** `measure-filter`, **F4** band derivation (stays a *proposal* a human locks in), **F5** `archive organize`. Four negative results recorded so they are not retried (star-colour locus, channel lock, optics normalisation, per-frame PTC bias fit). |
| [before-after-slider](before-after-slider.md) | **P0-P2a DONE, P3 PARTIAL** (implemented 2026-08-18; idea: user, after asking what the viewer's Enhance button does and finding it swaps the image with no way back). A draggable vertical divider comparing two renditions of the same frame. The name covers two features with different costs: **uniform A/B** (stretch / WB / curves / HDR / luma / debayer -- the same three textures sampled twice with two `StretchUBO`s, so it costs NOTHING and should ship regardless) and **pixel A/B** (enhance, wavelet re-sharpen, RAW-vs-STACK -- needs a second channel-texture set). Mechanism is **two scissored draws, not a shader branch**: `VkDynamicState.Scissor` is already dynamic and `RecordImageDraw` already binds set 0 (UBO) + set 1 (samplers) separately, so a split is scissor-left / bind A / draw, scissor-right / bind B / draw -- **no GLSL change and no SPIR-V re-bake**. Two findings shape it: (1) `AstroImageDocument` retains `UnstretchedImage` for life, so a displayed 3840x2160x3 frame already costs ~199 MB (99.5 CPU + 99.5 GPU) and keeping a second DOCUMENT as the before would double that for nothing -- **the before is a texture set, never a document** (99.5 MB, no CPU copy); (2) `UploadChannelTexture` reuses the texture and copies over it, so at `TryApplyPendingEnhance` the before pixels are ALREADY resident -- keeping them is not a copy but declining to overwrite (move three handles aside, let the enhanced upload allocate fresh), which inverts the cost comparison: handle transfer is instant and flat +99.5 MB, reload-on-press is a disk read + decode + upload peaking ~+300 MB. So the user's requested "two-way mode" is built as **a cache with a fallback, not a mode**: reload-on-press must exist anyway, keeping is a cache in front of it, decided by OBSERVATION (`CheckResult()` throws `OutOfDeviceMemory`; catch and fall back) plus a `GC.GetGCMemoryInfo().HighMemoryLoadThresholdBytes` pre-check, and evictable later -- which a once-decided mode cannot do. **Opening another document is invalidation, not eviction**: the before is no longer the before of what is displayed, so there is nothing to reload and the split must turn OFF -- enforced by an identity token checked where the comparison is DRAWN, never by clearing a flag on each of the eight paths that swap the image, and freed through the existing `StashForDispose` / `ReleaseCompletedTasks` post-frame drain (the load completes on a pool thread and the handles may still be in a command buffer). Note `DocumentCache` is already this shape for documents (weak refs, GC arbitrates) but the trick cannot be reused -- a `VkImage` is invisible to `MemoryLoadBytes`, hence `GC.AddMemoryPressure` while held. SHIPPED: the split lives in `ImageRendererBase` (surface-agnostic) because `PushClip` reaches `vkCmdSetScissor` immediately and DIR.Lib's clip stack already owns intersect+restore -- so the pipeline touches NO scissor and the whole thing is testable on the offline `RgbaImageRenderer` (12 tests, `ViewerSplitTests`, two sabotage-verified). Toolbar `Compare` button + `A` / `Shift+A` + a draggable divider. **Two UBO slots are mandatory**: both draws share one command buffer and the GPU reads the UBO at EXECUTE time, so one region would give both halves the last-written values -- silent, and looks like the comparison doing nothing. **The memory pre-check was wrong twice in its obvious form**, found by testing: `GC.GetGCMemoryInfo()` returns ZEROES until the first GC (so a fresh process reads "room" whatever the truth), and comparing current load against the threshold refuses a 100 MB cache on a box with 1.4 GiB spare -- ask whether THIS retention crosses it, and make the check `virtual` or machine-global load makes the same action behave differently minute to minute. **Remaining: P3's reload-on-press**, so a dropped cache today means the comparison is unavailable rather than restorable; P4 optional `R16_SFLOAT` halving; P5 more consumers. |
| [mount-safety-limits](mount-safety-limits.md) | **ALL PHASES DONE (P0, P2 2026-08-29; P1 editor UI, P1b for SkyWatcher, P3 both hosts, P4, P5 2026-08-30) and VERIFIED LIVE in the GUI 2026-08-30 (the watcher stopped a manual slew; verdict on the Home card and feed with no session via `MountLimitWatcher.VerdictFor`); open: hardware validation, tier label in the editor, TUI limits editor row** -- hour-angle and horizon safety limits, ported from GSServer's `CheckAxisLimits`. The gap it closes: nothing in TianWen stops a mount driving into its pier or the ground. `MeridianFlipDecision` bounds a *flip* (a scheduling choice about a target that keeps being imaged) and says nothing about the *mechanical* bound; outside a session there is no check at all. **P0** is the pure decider `MountLimits.Evaluate(hourAngleHours, altitudeDeg, isTracking, alreadyActed, config) -> MountLimitVerdict`, plus the `MountLimitConfiguration` record it cannot be written without (30 tests, three sabotages verified). <br>**Two deliberate departures from GSServer.** (1) The horizon test keys on **hour angle, not pier side**: altitude is maximal at upper transit and falls monotonically after, so `HA > 0` IS "descending" -- exactly, in both hemispheres, for fork and AltAz mounts that have no pier side at all, and *without* depending on a driver convention our own SkyWatcher driver gets wrong (it derives pier side from the Dec encoder and reports `Normal` while a GEM tracks west, which is the very case the gate exists to catch). (2) Warn and action are a threshold plus a non-negative **extra**, not two absolute numbers, because the two limits run in opposite directions (hour angle rises toward its limit, altitude falls toward its own) and an absolute pair can be edited into acting before it warns. GSS's own meridian shape (`HourAngleLimit + AxisTrackingLimit`), generalised to the horizon, which in GSS has no warning stage. <br>**The latch is load-bearing and is GSS's**: its `SlewState != SlewType.SlewPark` guard, commented "only hit this once while in limit" -- the check runs on a poll loop, so without it a park is re-commanded every tick and the park slew restarts forever, never arriving. Ours takes `alreadyActed` and downgrades to `Warn` rather than to clear, so the user keeps being told. **P2** (session enforcement via `PollDeviceStatesAsync`) shipped the same day as P0. **Corrected 2026-08-30: the meridian test takes the POINTING STATE** (`Normal ? -HA : HA`, `Unknown` = HA approximation) -- read on HA alone it stopped every rig ~30 min after a successful flip, the one place the flip clamp guarantees a rig will be; `FakeMountDriver.SetSideOfPierAsync` now forces a state so a test can hold a rig pre-flip, and the SkyWatcher driver's Normal-only axis solution surfaced and was fixed the same day (`SkyToSteps`: a goto chooses the solution from the target's HA, a sync keeps its half, so the fake now really flips on a re-slew); the LX200-base/SGP COMPUTED pointing state (HA-derived, silences the limit west of the meridian) remains open (TODO). **P1b DONE for SkyWatcher (2026-08-30)**: `IMountDriver.GetAxisAngleAsync` (degrees from the counterweight-down home, hemisphere-corrected, null on every other driver), `Evaluate` prefers it -- `|angle| - 90` is the counterweight above horizontal in either state, read with no clock, site or sync -- and `MountLimitVerdict.Basis` labels which tier answered. **P3** (`MountLimitWatcher`, enforcement with NO session running) shipped 2026-08-30 for `tianwen-server` only: host-agnostic core in `TianWen.Lib` (depends only on `IDeviceHub`/`IDeviceDiscovery`, no `Microsoft.Extensions.Hosting` dependency), matches a connected mount's URI against every known profile's `Mount` field each tick (there is no uniform "active profile" concept across hosts to key off instead), skips any mount a session already leases, and uses a per-mount-URI latch since more than one mount can be connected at once (10 tests, 2 sabotages: disabling the lease check, disabling the latch). **The GUI's own manual-slew path -- the scenario this phase exists for -- is still not wired**, since the GUI is not an ASP.NET host and needs a different integration than `MountLimitWatcherService`'s `BackgroundService`. **2026-08-30 close-out**: only a MEASURED pointing state drives the limit (`IMountDriver.PointingStateSource`, default Computed; LX200-base/SGP/Fake derive it from HA and now fall back to the hour-angle tier); a mount that stops tracking unasked is `MountLimitKind.DriverEnforced` and ends the run as a limit, not a fault (P5); the verdict is telemetry -- `ISessionTelemetry.MountLimitVerdict`, `MountLimitDto` (nullable), mirror, `LiveSessionState`, Home board cell + row, both feeds on class transitions (P4); the GUI drives `MountLimitWatcher` from `Program.cs` (P3); the editor is `PanelSection.MountLimits` plus a "Meridian Flip" config group whose deadline shows the limit's clamp as a caveat (P1); the watcher matches profiles by `Uri.DeviceKey`; SkyWatcher `SetSideOfPierAsync` is GSS's forced flip. |
| [gss-parity-audit](gss-parity-audit.md) | **AUDIT COMPLETE (2026-08-29)** for the pulse/slew/queue band; two findings open. Walks the GSServer commits that touch code we ported and records which apply, so the same commits are not re-read next year. **Five do not**, and the reasons are structural rather than lucky: #89's reversing RA pulse cannot happen because our guide rate is a bounded five-member enum (`SkywatcherGuideRate`); `2c9cd16`/`f70d821` are races in GSS's command *queue*, which we do not have; `765bd26` is latency compensation we never added, and should stay unadded -- its guard skipped the wait at BOTH ends and made the pulse **zero length**; `6e6dba9` is test scaffolding for a position-counter asymmetry that our symmetric `EncodePosition`/`DecodePosition` do not have. <br>**Finding 1 (real): RA and Dec guide pulses are issued SEQUENTIALLY** in `GuideLoop` -- GSS #76. Both corrections come from ONE measurement at ONE instant on independent motors, so the pair costs `raMs + decMs` instead of `max(...)`, and at `MaxPulseMs = 2000` the worst case is 4 s of pulsing per frame, longer than a typical guide exposure. **The contract underneath is the more important half:** `IMountDriver.PulseGuideAsync` states no completion contract and its implementations disagree -- `SkywatcherMountDriverBase` (and `FakeSkywatcherMountDriver`, which inherits it) blocks for the full duration, while ASCOM, Alpaca, the DAL ST-4 path and `FakeMountDriver` all return once the pulse is COMMANDED. **The two fakes are on opposite sides of that**, which is worth knowing before writing a test against either. Blocking is not a slip: the boards have no "pulse for N ms" primitive, so the driver must send the `:I` restore or the `:K` itself and SOMETHING has to hold the duration -- GSS #76's actual shape is to hold it on its own task and return once commanded -- which is what **six of the eight** already do, what ASCOM states, and what `MeadeLX200ProtocolMountDriverBase` was explicitly built for (it records a local pulse-end and CAS-updates it, commented "Keep the latest end time (overlapping pulses)"). **Decided: `StartPulseGuideAsync`**, and the rename is the load-bearing half -- `PulseGuideAsync` says nothing about when it returns, which is how one driver came to block unnoticed. A blocking contract was considered and rejected: a proxy driver cannot honestly block (it would sleep locally, replacing an observation the remote driver really has with a guess), and the dual-axis objection to the async shape does not survive -- nothing wants per-axis waiting, the guide loop wants "no pulse is running", which one flag answers. The costs are real and filed: the SkyWatcher `:I` restore would have no caller to throw to (a failed restore leaves the axis at (1+/-f)x sidereal forever), simultaneous dual-axis is a CAPABILITY that ASCOM has no word for, and cancellation must reach an unawaited pulse. **Why the sequencing is unseen:** `GuideLoopTests` builds `FakeMountDriver` directly so it never blocks at all, and where the blocking driver IS driven (`FakeCameraMountCouplingTests`) the fake clock auto-advances `SleepAsync` -- so it happens exactly as on hardware, costs zero wall-clock, and nothing asserts on the fake time consumed. A test that catches it must assert on **fake time traversed per guide frame**. <br>**Finding 2: an "in progress" flag must be observable before the call that starts it returns** -- `fb6e682` and `2c9cd16` are that same defect in two places. Ours holds for pulses today only *because* the SkyWatcher driver blocks, so fixing finding 1 will port GSS's bug unless `_pulseGuideInFlight` is set before the write. <br>**Open, not fixed:** what our `j`/`E` pair does when an axis crosses the 24-bit counter boundary (~±0.93 rev from home on an EQ6). And `gss-oracle-transcripts.json` was generated from a stale clone, so it pins GSS's PRE-fix behaviour; regenerating against `origin/master` may legitimately turn `SkywatcherGssOracleTests` red. **Always read `origin/master`**: the local clone was 208 commits behind, enough that an earlier pass concluded "GSS has no horizon limit", which is wrong. |

---

## serial-probe: DONE ~90%

- Phase 1 (plumbing / core types): **DONE**; `ISerialProbe`, `ISerialProbeService`, `SerialProbeService`, `SerialProbeMatch`, `ProbeExclusivity`, `ProbeFraming` under `src/TianWen.Lib/Devices/Discovery/`. `ProbeAllAsync` wired into `DeviceDiscovery.RunSerialProbesAsync` (`DeviceDiscovery.cs:164`).
- Phase 2 (logger scopes): **DONE**; scope instrumentation lives in `SerialProbeService` itself; per-source migration made per-loop scopes moot.
- Phase 3 (two-tier pinned-port discovery): **DONE**; `PinnedSerialPort`, `IPinnedSerialPortsProvider`, `ActiveProfilePinnedSerialPortsProvider` all present; `ISerialProbe.MatchesDeviceHosts` implemented; GUI composition root wires the active-profile provider.
- Phase 4 (migrate all sources): **DONE**; `SkywatcherSerialProbe`, `OnStepSerialProbe`, `MeadeSerialProbe`, `IOptronSerialProbe`, `QhyCfw3SerialProbe`, `QfocSerialProbe` all migrated; each source's `DiscoverAsync` reads `probeService.ResultsFor(...)` with no per-port open.
- Phase 5 (cleanup): **DONE**; `WaitForSerialPortEnumerationAsync` / `EnumerateAvailableSerialPorts` callers removed from individual sources.
- Tests: **DONE**; `SerialProbeServiceTests`, `SkywatcherSerialProbeTests`, `OnStepQuirkProbeTests`, `ActiveProfilePinnedSerialPortsProviderTests`, `SerialPortNamesTests`.
- Evolution: `ISerialProbe` uses `ProbeFraming` (ordering within baud group) instead of the simpler `Exclusivity`-only model in the plan; beneficial addition, not a shortfall.

## skymap-milkyway: DONE ~75%

- Phase 1 (shader + full-screen quad): **DONE**; `_milkyWayPipeline` in `VkSkyMapPipeline.cs`, alpha push-constant, sun-altitude fade, `ShowMilkyWay` toggle with `[S]` key, `TryLoadMilkyWayTexture` in `SkyMapTab.cs`.
- Phase 2 (texture pipeline + shipped file): **DONE**; `milkyway.bgra.lz` exists at `src/TianWen.UI.Gui/Resources/`, 8-byte header format + lzip compression via `MilkyWayTextureBaker.cs`.
- Phase 3 (bake from real data): **PARTIAL**; `tools/generate_milkyway.cs` is the .NET rewrite; `MilkyWayTextureBaker` + `MilkyWayBakerInputs` implement Tycho-2 luminance binning with Gaussian blur + brightness curve. Planck dust extinction path scaffolded (`DustOpacity`, `--dust-opacity`) but HEALPix reprojection step missing, so real-data bake has not yet replaced the analytical placeholder.
- Phase 4 (Planck HEALPix reader): **PARTIAL**; `tools/reproject_planck_dust.cs` exists, but no FITS download / `ang2pix` verified; dust input path in `MilkyWayBakerInputs.LoadAsync` reads a pre-converted float32 file, not raw FITS.
- Phase 5 (brightness/atmosphere integration): **NOT STARTED**. Bortle index modulation, HSV saturation slider, horizon fade absent; only the Phase 1 sun-altitude fade is present.

## skymap-gpu-overlays: PARTIAL ~35%

- Phase 1 (mosaic panels + sensor FOV to `LinePipeline`): **DONE**; `BuildFovLines` at `VkSkyMapTab.cs:726` replaces the old CPU `DrawFovQuadrilateral`; sensor FOV + mosaic panels loop into `_fovFloats` and are emitted via `WriteToRingBuffer`/`DrawLineBuffer` (`VkSkyMapTab.cs:164-183`).
- Phase 2 (planet dots to GPU): **NOT STARTED**; no planet instance buffer, no dedicated planet pipeline.
- Phase 3 (batch glyph rendering): **NOT STARTED**; no `ComputeLabelPositions` or `DrawGlyphAtBaseline`; `DrawConstellationNames` / `DrawGridLabels` still use per-label CPU text path.
- Phase 4 (kill CPU RA/Dec grid scan): **NOT STARTED**; `poleInView` branch still active at `OverlayEngine.cs:620-631`; overlay path doesn't iterate `AllObjectIndices`.
- Phase 5 (cache meridian line geometry): **PARTIAL**; `_meridianFloats` / `_horizonFloats` exist (`VkSkyMapTab.cs:29-30`) and are keyed via `_lastStaticGeomKey` (`VkSkyMapTab.cs:39`), which invalidates only on LST change. Plan's explicit `lstThreshold` sub-pixel guard not separately called out but equivalent behaviour from `_cachedLiveTime` 1s granularity.

## tui-live-session-parity: PARTIAL ~20%

- Phase 1 (extract renderer-agnostic draw helpers): **NOT STARTED**; `GuideGraphRenderer` class exists (`src/TianWen.UI.Abstractions/GuideGraphRenderer.cs`) but no generic `Render<TSurface>` static helper; `RenderCompactGuideGraph`, `RenderVCurveChart`, `RenderTimeline`, `RenderMiniSparkline` remain instance methods on `LiveSessionTab<TSurface>` (lines 473, 713, 946, 1151).
- Phase 2 (timeline band in TUI): **NOT STARTED**; no timeline `Panel` row in `TuiLiveSessionTab`.
- Phase 3 (compact guide graph in TUI): **NOT STARTED**; `_guideBar` is plain `TextBar` with RMS text only (`TuiLiveSessionTab.cs:51`).
- Phase 4 (V-curve overlay): **NOT STARTED**; no `VCurveRenderer` call in TUI.
- Phase 5 (preview mount section): **DONE**; `BuildPreviewRows` appends a mount `HeadingRow` block with RA/Dec/pier/HA (`TuiLiveSessionTab.cs:445`); consumes `liveState.PreviewMountState`.
- Phase 6 (clickable mini-viewer toolbar): **NOT STARTED**; `_previewToolbar` is text-only (`TuiLiveSessionTab.cs:54`).
- Phase 7 (ABORT button + modal): **PARTIAL**; keyboard abort flow + `ShowAbortConfirm` state handling exist (`TuiLiveSessionTab.cs:575, 718-730`), shown as status-bar hint; no red `Canvas` modal overlay or `ActionRow` button.
- Phase 8 (per-camera sparkline colors): **NOT STARTED**; `BuildSparkline` at line 600 is monochrome Unicode.
- Phase 9 (integration tests): **NOT STARTED**.

## first-light-resilience: PARTIAL (meta only)

- Meta-plan document (sequencing, non-goals, prior-art inventory): **DONE** as a coordination document.
- Sub-plan 1 (driver resilience): **NOT STARTED**; see below.
- Sub-plan 2 (FOV obstruction detection): **NOT STARTED**; correctly gated on driver-resilience PR-B1..B3.
- Sub-plan 3 (site horizon mask): **NOT STARTED**; explicitly deferred.
- Cross-cutting conventions (`ITimeProvider.SleepAsync`, Device/Phase logger scopes, `SessionConfiguration` XML docs): in place from prior work, but the new wrappers that would use them haven't been written.

## driver-resilience: DONE ~95%

Shipped on branch `driver-resilience` as 6 commits (PR-B1..B6). See
[`../architecture/driver-resilience.md`](../architecture/driver-resilience.md) for the full architecture
with mermaid state diagrams.

- Phase 1 (`ResilientCall` helper): **DONE**; `src/TianWen.Lib/Sequencing/ResilientCall.cs` +
  `ResilientCallOptions.cs` + 11 tests. Presets: `IdempotentRead`, `NonIdempotentAction`,
  `AbsoluteMove`. PR-B1 `1ce1d56`.
- Phase 2 (hot-path audit / wrapping): **DONE**; idempotent reads (PR-B2 `be911f4`) and
  non-idempotent actions (PR-B3 `b1f02ba`) in `Session.Imaging.cs` and `Session.Focus.cs`.
  Uniform via `Session.ResilientInvokeAsync` which auto-wires `OnDriverReconnect`.
- Phase 3 (in-flight exposure handling): **PARTIAL**; `ImageLoopNextAction.DeviceUnrecoverable`
  added + escalation short-circuit wired. The explicit "GetImageAsync empty after
  reconnect → re-issue StartExposure without counting the frame" detector is not yet
  implemented; mechanical reconnect-counting is in place so two consecutive lost frames
  would trip the fault counter naturally.
- Phase 4 (escalation boundary / fault counter): **DONE**; `SessionConfiguration.DeviceFaultEscalationThreshold` (default 5) and `DeviceFaultDecayFrames` (default 10),
  `_driverFaultCounts` dict on Session, `OnDriverReconnect` / `DecayFaultCountersOnFrameSuccess` /
  `TryFindEscalatedDriver` helpers + 5 tests. PR-B4 `db7ba83`.
- Phase 5 (proactive reconnect from `PollDeviceStatesAsync`): **DONE**; new `PollDriverReadAsync` +
  `PollDriverReadAsyncIf` helpers track consecutive failures and fire one-shot `ConnectAsync`
  at threshold 3 + 4 tests. PR-B5 `1374cbb`. Also applied to cooling ramp polls in PR-B6 `20394c3`.
- Tests: **DONE**; `ResilientCallTests` (11) + `SessionFaultCounterTests` (11) = 22 new tests.
  1672 unit + 78 functional session tests pass.

Not shipped: lost-frame detector (Phase 3 optional extension). Everything else is in.

## fov-obstruction-detection: DONE ~95%

Shipped on branch `fov-obstruction-detection` after driver-resilience merged to main.

- Phase 1 (scout frame + compare): **DONE**; `ScoutResult`, `ScoutClassification`, `ScoutOutcome` in `src/TianWen.Lib/Sequencing/ScoutResult.cs`; `ScoutAndProbeAsync`, `ClassifyAgainstBaseline`, `TakeScoutFrameAsync`, `TryGetPreviousObservationBaseline` in `src/TianWen.Lib/Sequencing/Session.Imaging.Obstruction.cs`. Star-count classifier scales by `sqrt(exposure_ratio)` so a 10s scout vs. a 120s baseline compares correctly.
- Phase 2 (altitude-nudge disambiguation): **DONE**; `NudgeTestAsync` slews +N×half-FOV in declination, scouts again, and re-slews back in `finally` regardless of result. `ComputeWidestHalfFovDeg` derives nudge from camera pixel scale × focal length × NumX × BinX.
- Phase 3 (trajectory-aware wait): **DONE**; `EstimateObstructionClearTimeAsync` projects the target's natural altitude forward in 2-min steps until it reaches `current_alt + nudge_deg`, capped at 2 h lookahead. Returns `null` for setting targets.
- Phase 4 (integration with recovery loop): **DONE**; `RunObstructionScoutAsync` wraps the scout result + clear-time policy and returns `ScoutOutcome.Proceed` / `ScoutOutcome.Advance`. Wired into `ObservationLoopAsync` between `CenterOnTargetAsync` and `StartGuidingLoopAsync`. Transparency classification falls through to the existing `WaitForConditionRecoveryAsync`.
- Supporting config: **DONE**; `ScoutExposure`, `ObstructionStarCountRatioHealthy/Severe`, `ObstructionNudgeRadii`, `ObstructionClearFractionOfRemaining` added to `SessionConfiguration` with XML docs and sensible defaults.
- Tests: **DONE**; `SessionScoutClassifierTests` (6 unit tests for `ClassifyAgainstBaseline` + `ComputeWidestHalfFovDeg`) + `SessionScoutAndProbeTests` (5 functional tests covering first-observation, healthy, transparency-no-recovery, rising-target clear time, setting-target null clear time). All 1678 unit + 83 functional Session tests pass.

Not shipped: scout frames are not yet emitted via a `ScoutCompletedEventArgs` for the live-session UI (plan flagged as optional v1). Scout frames are written when the shared `SaveIntermediates` switch is on (default off), as `FrameType.Scout`.

## polar-alignment: DONE ~85%

Shipped on `main` between 2026-04-26 and 2026-05-01 (~50 commits).

- Phase 1 (`PolarAxisSolver` math + tests): **DONE**; `src/TianWen.Lib/Astrometry/PolarAxisSolver.cs` + `PolarAxisSolverTests` + `PolarAlignmentHelpersTests`. Two-frame chord geometry with chord-angle sanity check (`6f345a3`).
- Phase 2 (`PolarAlignmentSession` orchestrator + capture sources + ramp + integration tests): **DONE**; `src/TianWen.Lib/Sequencing/PolarAlignment/PolarAlignmentSession.cs`, concrete `ICaptureSource` shims for main camera and PHD2, `PolarAlignmentSessionTests` + `PolarAlignmentRampIntegrationTests`. Adaptive exposure ramp with collapsed two-tier threshold (`37f538f`), backoff on capture failure (`fca6024`), `MinStarsForSolve` tightened to 25 (`fda1be7`), axis reverse-restore on dispose (`6f345a3`, `56941b8`).
- Phase 3a (generic `WcsAnnotationLayer`): **DONE**; `src/TianWen.UI.Abstractions/Overlays/PolarAnnotationBuilder.cs` + reusable `SkyMarker`/`SkyRing`/`SkyEdge` primitives, contributed to live preview alongside the existing WCS grid (`06e926e`, `667cb55`).
- Phase 3b (polar-align mode in `LiveSessionTab`): **DONE**; mode toggle, signals, `PolarAlignmentActions` helper, side-panel widgets, ring labels, meridian + prime-vertical lines, raw-error display, polar-adjuster knob jitter (`4d45462`, `d3c81a3`, `345c910`, `9666359`).
- Phase 4 (TUI parity): **DONE**; text gauges + ASCII arrow + status line driven by the same `LiveSessionState` fields (`4d45462`).
- Phase 5 (PHD2 path): **DONE**; `Save Images` integration verified end-to-end (`4d45462`).
- Refinement loop extras (beyond plan): `IncrementalSolver` fast path with frozen-seed quad matching + adaptive quad tolerance (`c261a8b`, `9e1a371`, `56d9e82`, `6ea03b7`), Jacobian live tracker (`b72ab8f`), sidereal-time normalisation (`7d51671`), reference-frame averaging (`570e4d9`), live WCS binding + CT fix (`03a0e73`), aperture-aware optics (`bbe3d54`), per-stage timing instrumentation (`a2ce471`).

Not shipped (TODO.md lines 142, 146):
- **Refraction-corrected apparent pole.** `PolarAlignmentSession.cs:658-659` literally sets `RefractedPoleRaHours: trueRa` / `RefractedPoleDecDeg: trueDec`; the apparent-pole rings draw on the true pole. Decomposition gauges already use refraction-aware math (correct numbers), only the overlay center is stale. Matters most at lat ≤ 35°.
- **Live site pressure/temperature.** DONE (`feat/top-5-todo`, 2026-06-12) via `SiteConditions.Resolve(IWeatherDriver?)` -> the 1010/10 hardcode is gone; live weather feeds refraction, else pressure derives from elevation. Unblocks polar-alignment refraction. (Values deliberately not profile-stored -- they vary.)

## catalog-binary-format: DONE

Plan now leads with **Option D** (ASCII-separated text + `tools/preprocess-catalog.ps1`
MSBuild step) instead of Option A (MessagePack). Tycho2 stays untouched (parallel
multi-member lzip already optimal; gzip would lose parallelism, see plan doc).

**Phase 1 (Option D format migration); SHIPPED end-to-end (2026-05-04):**

- Option D preprocessor (`tools/preprocess-catalog.ps1`): **DONE**; pwsh script reads `*.json.lz` / `*.csv.lz`, parses, re-emits with `0x1D`/`0x1E`/`0x1F` separators (G17 doubles, invariant culture), gzip-compresses to `*.gs.gz` via `System.IO.Compression.GZipStream` (Optimal). Encoder + decoder match one-to-one, no external lzip binary needed.
- MSBuild `<Exec>` target: **DONE**; `<CatalogPreprocess>` items in `TianWen.Lib.csproj`, batched `Inputs="@(...)" Outputs="@(...->...)"` so the preprocessor only re-runs when the source `.json.lz` is newer than the `.gs.gz`.
- Runtime reader: **DONE**; `AsciiRecordReader` (`src/TianWen.Lib/IO/AsciiRecordReader.cs`) provides `EnumerateRecords` / `TakeField` / `ReadDouble` / `ReadNullableDouble` / `ReadStringArray` over `ReadOnlySpan<byte>`. `ParseSimbadGsAsync` + `MergeNgcGsData` decode the migrated catalogs.
- Shared `AsciiRecordReader` helper: **DONE**.
- Catalog rollout: **DONE**, all 13 SIMBAD catalogs (HR, GUM, RCW, LDN, Dobashi, Sh, Barnard, Ced, CG, vdB, DG, HH, Cl) and both NGC (NGC, NGC.addendum) shipped on the `.gs.gz` path.
- Compression swap (lzip → gzip on `.gs` files): **DONE**; bench showed BCL GZipStream is 4-8× faster on small single-stream payloads, +2.3 % on size (1,001 KB → 1,024 KB embedded).
- Tests: **DONE**, 1,841 unit tests pass against the migrated format; `CelestialObjectDBBenchmarkTests` prints per-phase breakdown.
- Secondary lookup speed improvement (`ArrayPool` BFS / frozen-dict transitive closure): **SUPERSEDED** by `_crossIndexClosures`, see Phase 2C below.

**Phase 1 numbers (post-migration, post-gzip):**

| Build | First `InitDBAsync` |
|---|---|
| Release + warm runtime (test bench) | **716–906 ms** (run-to-run variance) |
| Debug + cold disk + cold JIT (GUI cold launch) | **2,411 ms** |

The hot phases are now dict-mutation work, not parse work; what Phase 2 is for.

**Phase 2 (pre-bake init state): DONE (2A + 2B + 2C):**

- 2A SHIPPED (2026-05-05): `tools/precompute-hd-hip-cross/` bakes the post-`BuildHdHipCrossIndicesViaTyc` state into `hd_hip_cross.bin.gz` (~2.4 MB embedded). Runtime apply takes ~110 ms (parallel SHA-256 input hash + gzip read + dict mutation) vs ~460 ms live compute. Net saving: ~350 ms on the hd-hip-cross phase. CI guards in `HdHipCrossSnapshotTests` catch staleness + algorithm-vs-snapshot drift. Re-bake via `pwsh tools/precompute-hd-hip-cross.ps1`.
	- 2B SHIPPED (2026-05-05): `tools/precompute-simbad-merge.ps1` bakes the post-SIMBAD-merge state into `simbad_merge.bin.gz` (~754 KB embedded). Runtime apply skips ~180 ms of parse + dict-mutation work across 14 catalogs. Same hash-verify-then-apply pattern as 2A. CI guards in `SimbadMergeSnapshotTests` (commit `8da9b16`). Re-bake via `pwsh tools/precompute-simbad-merge.ps1`.
	- 2C SHIPPED in both halves (2026-09-03 close-out). **Tycho-2 bulk load: 0.3 ms of init**, measured 2026-08-31 -- `ExpandTycho2` plus the background task closed it, and `ExpandTycho2CrossRef` then caught `hip_to_tyc`/`hd_to_tyc` still lzip-decompressing at runtime (274.7 ms). **Init 587 -> 343 ms, the blocking join phase 269.9 -> 0.0 ms.**
	- **The BFS half was superseded, not skipped** (`a7c7f9a2`, 2026-08-09): `_crossIndexClosures` memoises `TryGetCrossIndices` lazily, which is the frozen-dict option's outcome without its ~50 ms of init, and it removes the walk the `ArrayPool` option would have kept. Found by measuring the sky map's full-sky overlay gather (78.1 MB a pass, **53.89 MB of it in `TryGetCrossIndices`**) -> **22.98 MB, 105.7 -> 84.8 ms, Gen0/1000 8833 -> 1833**. The missing-row early-out shipped with it and answers only a quarter of calls, so it is not what did the work.
	- **Nothing actionable remains.** `hd-hip-cross` at 121.8 ms is the largest surviving phase and IS 2A's deserialise-and-apply path, not the 330 ms recompute it replaced. Phase 2 targeted 280-400 ms; init went 729 -> 343 ms.

## gpu-stretch-tests: DONE ~90%

GPU-vs-CPU pixel-parity tests for the stretch pipeline. Enabled by the offscreen
path that shipped in `SdlVulkan.Renderer` (`VulkanContext.CreateOffscreen` +
`VkRenderer.BeginOffscreenFrame` + `VulkanContext.ReadbackOffscreenRgba`).

- Phase 1 (smoke test + class fixture): **DONE**; `OffscreenGpuFixture.cs` owns
  `(instance, ctx, renderer, pipeline)` and skips with a diagnostic when Vulkan
  init fails; `GpuStretchPipelineTests.cs` carries the smoke `[Fact]`.
- Phase 2 (skip-when-unavailable + CI driver install): **DONE**; `.github/workflows/dotnet.yml`
  installs `mesa-vulkan-drivers libvulkan1 vulkan-tools` and emits a
  `vulkaninfo --summary` diagnostic before build. `IsVulkanInitFailure` short-circuits
  to `Assert.Skip` on missing ICD.
- Phase 3 (8 Vela theory cases parity): **DONE**; `GpuMatchesCpuForVelaStretchCases`
  runs the same 8 inline cases the CPU `StretchTests_NewPipeline` covers; tolerances
  `mean < 1.5`, `max < 16`, outliers `< 1%`.
- Phase 4 (synthetic SPCC GPU verification): **DONE**; `GivenSyntheticSpccField_GpuRenderMatchesCpuRender`
  runs the full SPCC pipeline (filter throughput, Tycho-2 match, pivot1, curves, HDR)
  through both paths; additional Luma-weighting, luma-blend, HDR-normalize, sensor-matched
  theory tests included.
- Phase 5 (opt-in `[Trait("Category","GPU")]` CI job): **REPLACED**; GPU tests run inside
  the regular `test-unit` job (lavapipe is fast enough on the test images); no separate
  trait filter or split job needed. Functionally equivalent to the plan, simpler infra.
- Follow-up D (primitives parity): **DONE**; `VkRendererPrimitiveTests.cs` covers
  rectangle/ellipse/circle/line vs `RgbaImageRenderer`.
- Follow-up F (line tessellation): **DONE**; `SkyMapLineTessellationTests.cs` asserts
  GPU line vertex output without rasterisation.
- Not shipped: follow-ups A (Bayer demosaic), B (histogram), C (WCS grid), E (sky map
  stars), G (milky way), H (overlay ellipses). All optional extensions to the same
  `OffscreenGpuFixture`.

## icc: DONE ~95%

ICC profile tagging across our display output paths. Sibling work landed under
`../sharpastro/StbImageSharp/` as new packages `SharpAstro.Color.Icc`, `SharpAstro.Jpeg`,
and a breaking-change bump on `SharpAstro.Tiff`.

- Phase 1 (`SharpAstro.Tiff` breaking change to `ReadOnlyMemory<byte>` `IccProfile`): **DONE**;
  consumed at `SharpAstro.Tiff 3.0.*` in `Directory.Packages.props:54`.
- Phase 2 (new `SharpAstro.Jpeg` library with `JpegIccInjector.EmbedIccProfile`): **DONE**;
  sibling project exists; consumed at `SharpAstro.Jpeg 3.0.*` (single-version family
  alongside Tiff/Png/Color.Icc, not the `1.0.*` originally planned).
- Phase 3 (publish to NuGet): **DONE**, all four packages resolve from nuget.org.
- Phase 4a (display TIFF helper consolidation): **DONE**; `src/TianWen.Lib.Tests/Helpers/DisplayImageWriter.cs`
  centralises sRGB-tagged 8-bit RGB TIFF + PNG output for the three test files the plan
  flagged. Plan's working name `TestDisplayTiffWriter.cs` was renamed to `DisplayImageWriter.cs`
  since it also handles PNG.
- Phase 4b (`NinaImageEndpoints` JPEG injection): **DONE**; `src/TianWen.Hosting/Api/NinaV2/NinaImageEndpoints.cs:133`
  wraps `WriteJpg` output through `JpegIccInjector.EmbedIccProfile(..., IccProfiles.SRgbV4)`.
- Phase 4c (`Image.WriteTiffAsync` stays untagged): **HONOURED**; scientific 32-bit float
  TIFF deliberately ships with no ICC tag per scope decision 2.
- Phase 4d (production PNG writers): **N/A**; no production display-PNG paths in
  `TianWen.Lib` proper. Test/tools PNGs use `DisplayImageWriter.EncodePng` when tagging
  is desired.
- Not shipped: ICC consumption on read (out-of-scope per "Non-goals"), multi-segment
  APP2 (deferred until we have a non-trivial device profile).

---

## Bottom line

- **Shipped:** serial-probe (merged), driver-resilience (merged), fov-obstruction-detection (merged), polar-alignment (merged, refraction polish pending), gpu-stretch-tests (Phases 1-4 + follow-ups D & F), icc (all four phases), catalog-binary-format (Option D + Phase 2 in full).
- **Substantially advanced:** milkyway (Phases 1-2 done, 3-4 scaffolded).
- **Partially started:** skymap-gpu-overlays (Phase 1 + cache hit), tui-live-session-parity (preview mount section + partial abort flow).
- **Essentially untouched:** site horizon mask (sub-plan 3 of first-light-resilience) deferred until operational data warrants it.

**First-light-resilience status:** 2 of 3 sub-plans shipped (driver resilience + FOV obstruction).
Sub-plan 3 (static azimuth horizon mask) is intentionally deferred, only spin up if 1+2
in production show too many runtime scout trips against known obstructions.

## stacking: DONE ~85%

The original phasing table (Phases 1-12) is shipped: `Image` arithmetic, masters,
calibrator, registrator, normalizer, rejectors (sigma + winsorized + LFC +
percentile + minmax), the in-memory `Integrator`, and FITS write via
`IntegrationFitsWriter` (Phase 9). End-to-end validated on real datasets via
`StackingEndToEndManualTest` (skips when `C:\temp\stack\` is absent so CI stays
green).

Phase 9 caveat: the `IIntegrationSink` interface + `ArraySink` abstraction
mentioned in the plan are *not* shipped -- only the concrete static
`IntegrationFitsWriter` plus a sidecar `<master>.rejection.fits` companion file
for the rejection-map diagnostic. The interface was meant as a seam for Phase
10's `MemoryMappedFitsSink`; with Phase 10 deferred, pre-introducing the
interface for a single in-memory consumer would be YAGNI -- right time to
extract is when MMF lands. MEF (multi-extension FITS) is also deferred per the
file's own xmldoc: most FITS viewers don't render the second HDU, so two files
keep the rejection diagnostic usable.

**Phase 8 ("tile integrator") evolved into a strategy-pattern selector** with
six executors picked at runtime by an `IntegrationStrategySelector` against an
`IntegrationProbe` (physical RAM + disk + frame geometry) gated by a
`ResourceBudget` (default 75% RAM / 80% disk). Shipped sub-phases this session
(2026-05-16):

- **8.0** `bbab3c8`: `IntegrationJob` v2 surface + scaffolding.
- **8.1** `f4a7013`: `PartialFitsReader` (mmap'd FITS sub-region reader, 9 unit tests).
- **8.2** `330b4b3`: `Image.WarpRegionAsync` + strip-pipelined `TilePipelinedStrategy.RunAsync`.
- selector `da67544`: physical-RAM hard gate + free-RAM soft penalty.
- **8.2 perf** `7415671`: strong+weak cached debayered frames → ~10× speedup (Liberty 120s 10.9 min → 1.6 min).
- refactor `40fce57`: `FrameCache` extracted as shared helper.
- **8.3** `7ece80b`: BDN bench: PartialFitsReader **36× faster** on tile reads, 6000× less alloc.
- cache wire `e17d585`: `StreamingFrameReader.SetCachedImage` + FrameCache wired into FootprintStaged + Float16Staged.

**Still pending:**
- Phase 10 `MemoryMappedFitsSink` for tier-3 mosaic outputs (not user-blocked).
- Phase 13 `tianwen stack` CLI orchestrator (`StackingEndToEndManualTest` is the
  flying sketch).
- Phase 14 `LiveStacker` Welford engine + Phase 15 session integration
  (`LiveAccumulatorStrategy` is the selector-level placeholder).
- Phase 8.2 sub-region debayer + halo-aware per-tile warp (the cached-debayered
  fast path already covers the common roomy-host case; this is the next
  optimization tier when memory is genuinely tight AND the cache misses dominate).
- SIMD byte-swap in `PartialFitsReader` for full-image reads (production hot
  path only does tile reads where mmap already wins 36×, so low priority).

See `stacking.md` § "Phase 8 implementation status (2026-05-16)" for the
cold-start guide to the codebase: every file that holds the strategy machinery,
the test entry points, and the benchmark numbers worth remembering.

## altaz-mount-support: Phase 0 DONE (Phases 1-3 NOT STARTED)

SkyWatcher mounts like the AZ-GTi are dual-mode (equatorial on a wedge / alt-azimuth flat).
The driver is equatorial-only; this plan covers making alt-az *safe* (shipped) then *usable*
(deferred). See [altaz-mount-support.md](altaz-mount-support.md) for the full gap analysis.

- **Phase 0 (safe, make an alt-az mount un-mis-pointable): DONE** (2026-06-22, PRs #47 + #48).
  - `?alignment=GermanPolar|Polar|AltAz` device setting (`SkywatcherDevice` / `DeviceQueryKey.Alignment`),
    default `GermanPolar`. AZ vs EQ is **not protocol-detectable** (same model code, `:q CanAzEq`
    only means "supports both"), so it's a user setting mirroring GSServer, never auto-detected.
  - `AltAz` is **report-only**: `GetAlignmentAsync` returns it and pier reads return `Unknown`, but
    `BeginSlewRaDecAsync` / `SyncRaDecAsync` / `SetTrackingAsync(true)` are **refused** via
    `SkywatcherMountDriverBase.EnsureEquatorialAlignment` (throws `NotSupportedException`).
    `MaybeSyncToPoleAfterSiteSetAsync` is skipped for alt-az (home is the raw encoder zero, az 0
    / alt 0, not the equatorial pole).
  - Meridian flip is now **GEM-only**: `ImagingLoopAsync` reads `AlignmentMode.GermanPolar` and
    fork/AltAz skip the whole flip cycle (PR #47). Pinned by the `Polar`/`AltAz` `[Theory]` in
    `SessionObservationLoopTests` (`MeridianFlipCount == 0`).
  - Tests: `FakeSkywatcherMountDriverTests` (`GivenAltAzAlignmentThenReportedButCoordinateOpsRefused`,
    `GivenNoAlignmentQueryThenDefaultsToGermanPolar`) + the GEM-only flip `[Theory]`.
- **Phase 1 (correct GOTO + tracking + position, visual/EAA/plate-solve, no imaging): NOT STARTED.**
  Az/Alt↔encoder-step transforms, `BeginSlewToTargetAsync` pier-gate bypass for alt-az (an
  `IMountDriver`-layer change that also unblocks ASCOM/Alpaca alt-az mounts), dual-axis predictor
  tracking, and Az/Alt→RA/Dec position reads. Tracked in TODO.md.
- **Phase 2 (alt-az guiding): NOT STARTED**, gated on Phase 1 (field rotates continuously, so the
  fixed-angle calibration assumption breaks; likely short unguided subs or a different guide model).
- **Phase 3 (long-exposure alt-az imaging): BLOCKED** on field-rotation handling, needs a mechanical
  derotator (`IRotatorDriver` / `DeviceType.Rotator`, which doesn't exist, see the `rotator` plan +
  TODO.md) or software derotation in the stacker.

**Recommendation (from the plan):** Phase 0 makes the alt-az case safe rather than silently wrong;
most AZ-GTi *imagers* run EQ-on-a-wedge anyway (already works as `GermanPolar`). Pursue Phase 1 only
on demand; defer Phase 3 until rotator support lands.
